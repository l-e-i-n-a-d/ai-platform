# Kubernetes Integration

AKS is the execution environment for isolated agent workspaces.

Helm is the preferred deployment packaging mechanism.

Security boundaries should be enforced using Kubernetes and Azure identity mechanisms rather than trusting agent behavior.
