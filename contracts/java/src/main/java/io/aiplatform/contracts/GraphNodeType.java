package io.aiplatform.contracts;

/**
 * Discriminates the branches of the union. The members are the union of the values each branch
 * permits.
 *
 * Generated by .github/scripts/codegen.py from schemas/graph/graph.schema.json.
 *
 * DO NOT EDIT. Changes belong in the schema; this file is regenerated and CI
 * fails if the committed output differs (ADR-0024 §3).
 */
public enum GraphNodeType {
    CONTEXT("context"),
    AGENT("agent"),
    TOOL("tool"),
    APPROVAL("approval"),
    DECISION("decision"),
    LOOP("loop"),
    TERMINAL("terminal");

    private final String wire;

    GraphNodeType(String wire) {
        this.wire = wire;
    }

    /** The value as it appears in JSON, which is not always the constant name. */
    public String wire() {
        return wire;
    }

    public static GraphNodeType fromWire(String value) {
        for (GraphNodeType candidate : values()) {
            if (candidate.wire.equals(value)) {
                return candidate;
            }
        }
        throw new IllegalArgumentException(
                "unknown GraphNodeType value: " + value);
    }
}
