"""Contract models generated from the canonical JSON Schemas.

Generated by .github/scripts/codegen.py from schemas/**/*.schema.json.

DO NOT EDIT. Changes belong in the schema; this file is regenerated and CI
fails if the committed output differs (ADR-0024 §3).
"""

from __future__ import annotations

from dataclasses import dataclass
from enum import Enum
from typing import Any

__all__ = [
    "Agent",
    "AgentCapabilityCeiling",
    "AgentDefaultBudgets",
    "AgentDefaultModelPolicy",
    "AgentExecutionRequest",
    "AgentExecutionRequestBudgets",
    "AgentExecutionRequestModelPolicy",
    "AgentExecutionResult",
    "AgentExecutionResultFailure",
    "AgentExecutionResultStatus",
    "AgentOutputContractStyle",
    "Approval",
    "ApprovalKind",
    "ApprovalRequiredKind",
    "ApprovalStatus",
    "ArtifactRef",
    "ArtifactRefClassification",
    "ArtifactRefClassificationArtifactType",
    "ArtifactRefClassificationProvenance",
    "ArtifactRefClassificationRetentionTier",
    "ArtifactRefClassificationSensitivity",
    "ArtifactRefEncoding",
    "AuditEntry",
    "AuditEntryActor",
    "AuditEntryAssurance",
    "AuditEntryDecision",
    "CapabilityGrant",
    "CapabilityGrantAllowedToolsItem",
    "CapabilityGrantBudget",
    "CapabilityGrantExternalScope",
    "CapabilityGrantSubject",
    "Checkpoint",
    "CheckpointWorkspaceState",
    "CheckpointWorkspaceStateRepositoriesItem",
    "CommandProfile",
    "CommandProfileParametersItem",
    "CommandProfileParametersItemType",
    "ConditionComparison",
    "ConditionComparisonOp",
    "ConditionComparisonRight",
    "ConditionComparisonRightSource",
    "ConditionComposition",
    "ConditionCompositionOp",
    "ConditionConstant",
    "ConditionExpression",
    "ConditionExpressionOp",
    "ConditionExpressionRight",
    "ConditionExpressionRightSource",
    "ConditionMembership",
    "ConditionMembershipOp",
    "ConditionNegation",
    "ConditionPresence",
    "ConditionPresenceOp",
    "ConditionReference",
    "ConditionReferenceSource",
    "ContextBundle",
    "ContextBundleBudget",
    "ContextBundleExcludedItem",
    "ContextBundleExcludedItemReason",
    "ContextBundleItem",
    "ContextBundleItemSourceSystem",
    "ContextBundleItemStrategy",
    "ContextBundleItemTrustClass",
    "ContextBundlePins",
    "ContextBundlePinsIssuesItem",
    "ContextBundlePinsPagesItem",
    "ContextBundlePinsRepositoriesItem",
    "Correlation",
    "ExecutionRequest",
    "ExecutionResult",
    "ExecutionResultNetworkDenialsItem",
    "ExecutionResultStatus",
    "FailureCategory",
    "Graph",
    "GraphAgentNode",
    "GraphAgentNodeBudgets",
    "GraphAgentNodeModelPolicy",
    "GraphAgentNodeRequestedCapabilities",
    "GraphApprovalNode",
    "GraphApprovalNodeRequiredKind",
    "GraphContextNode",
    "GraphDecisionNode",
    "GraphEdge",
    "GraphLoopNode",
    "GraphNode",
    "GraphNodeBudgets",
    "GraphNodeModelPolicy",
    "GraphNodeOutcome",
    "GraphNodeRequestedCapabilities",
    "GraphNodeRequiredKind",
    "GraphNodeType",
    "GraphRetryPolicy",
    "GraphRetryPolicyBackoff",
    "GraphRetryPolicyBackoffStrategy",
    "GraphRetryPolicyRetryableCategoriesItem",
    "GraphRun",
    "GraphRunFailure",
    "GraphRunLease",
    "GraphRunStatus",
    "GraphRunWorkItemRef",
    "GraphTerminalNode",
    "GraphTerminalNodeOutcome",
    "GraphToolNode",
    "ModelRequest",
    "ModelRequestBudgets",
    "ModelRequestContentBlock",
    "ModelRequestContentBlockType",
    "ModelRequestMessagesItem",
    "ModelRequestMessagesItemRole",
    "ModelRequestParams",
    "ModelRequestProviderOptions",
    "ModelRequestToolsItem",
    "ModelResponse",
    "ModelResponseContentItem",
    "ModelResponseContentItemType",
    "ModelResponseCost",
    "ModelResponseError",
    "ModelResponseErrorCategory",
    "ModelResponseFinishReason",
    "ModelResponseToolCallsItem",
    "ModelResponseUsage",
    "NetworkPolicy",
    "NetworkPolicyMode",
    "NodeRun",
    "NodeRunIndeterminateReason",
    "NodeRunNodeType",
    "NodeRunStatus",
    "NodeRunStopReason",
    "PathScopeEntry",
    "RepoRef",
    "RepositoryRecord",
    "RepositoryRecordCapabilityCeiling",
    "RepositoryRecordCapabilityCeilingBudgetCeiling",
    "RepositoryRecordContextPolicy",
    "RepositoryRecordStatus",
    "RepositoryRecordVerification",
    "ResourceLimits",
    "ResourceUsage",
    "SideEffectClass",
    "Tool",
    "ToolApprovalPolicy",
    "ToolApprovalPolicyMode",
    "ToolCostHint",
    "ToolIdempotency",
    "ToolRequest",
    "ToolResult",
    "ToolResultDenial",
    "ToolResultDenialReason",
    "ToolResultFailure",
    "ToolResultStatus",
    "ToolScopeRequirements",
    "TrustLevel",
    "Workspace",
    "WorkspaceExecutionMode",
    "WorkspaceLease",
    "WorkspaceSpec",
    "WorkspaceStatus",
]


class AgentExecutionResultStatus(str, Enum):
    """
    Maps onto the failure categories of ADR-0008. The runtime does not invent categories and
    does not decide retryability.

    Source: schemas/agent/agent-execution-result.schema.json
    """

    COMPLETED = "COMPLETED"
    BUDGET_EXHAUSTED = "BUDGET_EXHAUSTED"
    MODEL_ERROR = "MODEL_ERROR"
    TOOL_ERROR = "TOOL_ERROR"
    RUNTIME_ERROR = "RUNTIME_ERROR"
    CANCELLED = "CANCELLED"


class AgentOutputContractStyle(str, Enum):
    """
    STRUCTURED: the agent is expected to produce output validating against the node's declared
    output schema. FREEFORM: prose, permissible only where no downstream transition depends on
    the shape.

    Source: schemas/agent/agent.schema.json
    """

    STRUCTURED = "STRUCTURED"
    FREEFORM = "FREEFORM"


class ApprovalKind(str, Enum):
    """
    LOCAL_OPERATOR_CONSENT is self-asserted: the platform can attest that someone at the
    keyboard consented, not who they were. EXTERNAL_ATTESTATION borrows identity from a system
    that has it — a GitHub review, a Jira transition — and is the stronger form precisely
    because the platform is not the one asserting it.

    Source: schemas/approval/approval.schema.json
    """

    LOCAL_OPERATOR_CONSENT = "LOCAL_OPERATOR_CONSENT"
    EXTERNAL_ATTESTATION = "EXTERNAL_ATTESTATION"


class ApprovalRequiredKind(str, Enum):
    """
    What policy demanded, computed as the strictest of the tool's side-effect class, the
    repository trust level and the graph node definition (ADR-0009 §2). Any layer may require a
    stronger kind; none may require a weaker one.

    Source: schemas/approval/approval.schema.json
    """

    LOCAL_OPERATOR_CONSENT = "LOCAL_OPERATOR_CONSENT"
    EXTERNAL_ATTESTATION = "EXTERNAL_ATTESTATION"


class ApprovalStatus(str, Enum):
    """
    Generated from schemas/approval/approval.schema.json.

    Source: schemas/approval/approval.schema.json
    """

    PENDING = "PENDING"
    APPROVED = "APPROVED"
    REJECTED = "REJECTED"
    EXPIRED = "EXPIRED"
    REVOKED = "REVOKED"


class ArtifactRefClassificationArtifactType(str, Enum):
    """
    Generated from schemas/artifacts/artifact-ref.schema.json.

    Source: schemas/artifacts/artifact-ref.schema.json
    """

    TRANSCRIPT = "TRANSCRIPT"
    PROMPT = "PROMPT"
    COMPLETION = "COMPLETION"
    CONTEXT_BUNDLE = "CONTEXT_BUNDLE"
    PATCH = "PATCH"
    BUILD_LOG = "BUILD_LOG"
    CI_LOG = "CI_LOG"
    TOOL_OUTPUT = "TOOL_OUTPUT"
    EVALUATION = "EVALUATION"
    REPORT = "REPORT"
    AUDIT_EXPORT = "AUDIT_EXPORT"


class ArtifactRefClassificationProvenance(str, Enum):
    """
    Generated from schemas/artifacts/artifact-ref.schema.json.

    Source: schemas/artifacts/artifact-ref.schema.json
    """

    PLATFORM_GENERATED = "PLATFORM_GENERATED"
    RETRIEVED = "RETRIEVED"
    EXECUTED_OUTPUT = "EXECUTED_OUTPUT"
    MODEL_OUTPUT = "MODEL_OUTPUT"


class ArtifactRefClassificationRetentionTier(str, Enum):
    """
    Generated from schemas/artifacts/artifact-ref.schema.json.

    Source: schemas/artifacts/artifact-ref.schema.json
    """

    EPHEMERAL = "EPHEMERAL"
    RUN_LIFECYCLE = "RUN_LIFECYCLE"
    REPRODUCIBILITY = "REPRODUCIBILITY"
    AUDIT = "AUDIT"


class ArtifactRefClassificationSensitivity(str, Enum):
    """
    Generated from schemas/artifacts/artifact-ref.schema.json.

    Source: schemas/artifacts/artifact-ref.schema.json
    """

    PUBLIC = "PUBLIC"
    INTERNAL = "INTERNAL"
    SENSITIVE = "SENSITIVE"


class ArtifactRefEncoding(str, Enum):
    """
    Generated from schemas/artifacts/artifact-ref.schema.json.

    Source: schemas/artifacts/artifact-ref.schema.json
    """

    IDENTITY = "identity"
    GZIP = "gzip"
    ZSTD = "zstd"


class AuditEntryAssurance(str, Enum):
    """
    ADR-0016 §3. SELF_ASSERTED: the platform states it about itself and a local attacker could
    forge it. PLATFORM_ATTESTED: produced by a control-plane component and hash-chained.
    EXTERNALLY_VERIFIABLE: corroborated by a system outside this machine, such as a GitHub
    commit or a Jira transition. V1 is developer-attested and is not adversarial-grade; the
    field exists so that nobody later mistakes it for something stronger.

    Source: schemas/audit/audit-entry.schema.json
    """

    SELF_ASSERTED = "SELF_ASSERTED"
    PLATFORM_ATTESTED = "PLATFORM_ATTESTED"
    EXTERNALLY_VERIFIABLE = "EXTERNALLY_VERIFIABLE"


class AuditEntryDecision(str, Enum):
    """
    INDETERMINATE records that a write-ahead intent exists with no matching outcome. It is the
    only honest answer available after a crash mid-side-effect, and ADR-0008 §7 forbids
    auto-retrying it.

    Source: schemas/audit/audit-entry.schema.json
    """

    ALLOWED = "ALLOWED"
    DENIED = "DENIED"
    INDETERMINATE = "INDETERMINATE"


class CommandProfileParametersItemType(str, Enum):
    """
    Generated from schemas/tools/command-profile.schema.json.

    Source: schemas/tools/command-profile.schema.json
    """

    STRING = "string"
    INTEGER = "integer"
    BOOLEAN = "boolean"
    ENUM = "enum"


class ConditionComparisonOp(str, Enum):
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    EQ = "eq"
    NE = "ne"
    LT = "lt"
    LTE = "lte"
    GT = "gt"
    GTE = "gte"


class ConditionComparisonRightSource(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/graph/condition.schema.json
    """

    NODEOUTPUT = "nodeOutput"
    RUNVARIABLE = "runVariable"


class ConditionCompositionOp(str, Enum):
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    AND = "and"
    OR = "or"


class ConditionExpressionOp(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/graph/condition.schema.json
    """

    EQ = "eq"
    NE = "ne"
    LT = "lt"
    LTE = "lte"
    GT = "gt"
    GTE = "gte"
    IN = "in"
    NOTIN = "notIn"
    EXISTS = "exists"
    NOTEXISTS = "notExists"
    AND = "and"
    OR = "or"
    NOT = "not"


class ConditionExpressionRightSource(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/graph/condition.schema.json
    """

    NODEOUTPUT = "nodeOutput"
    RUNVARIABLE = "runVariable"


class ConditionMembershipOp(str, Enum):
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    IN = "in"
    NOTIN = "notIn"


class ConditionPresenceOp(str, Enum):
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    EXISTS = "exists"
    NOTEXISTS = "notExists"


class ConditionReferenceSource(str, Enum):
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    NODEOUTPUT = "nodeOutput"
    RUNVARIABLE = "runVariable"


class ContextBundleExcludedItemReason(str, Enum):
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    EVICTED_BUDGET = "EVICTED_BUDGET"
    POLICY_EXCLUDED = "POLICY_EXCLUDED"
    ACCESS_DENIED = "ACCESS_DENIED"
    STALE = "STALE"


class ContextBundleItemSourceSystem(str, Enum):
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    REPOSITORY = "REPOSITORY"
    JIRA = "JIRA"
    CONFLUENCE = "CONFLUENCE"
    GITHUB = "GITHUB"
    PLATFORM = "PLATFORM"
    RUN_STATE = "RUN_STATE"


class ContextBundleItemStrategy(str, Enum):
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    DIRECT = "DIRECT"
    PATH_MATCH = "PATH_MATCH"
    SEARCH = "SEARCH"
    LINK_TRAVERSAL = "LINK_TRAVERSAL"
    GRAPH_STATE = "GRAPH_STATE"


class ContextBundleItemTrustClass(str, Enum):
    """
    PLATFORM content is platform-authored. UNTRUSTED is everything retrieved from a repository,
    issue, page or pull request, and is treated as data that may attempt to influence the model.
    It is never rendered where the protocol expects platform-controlled fields.

    Source: schemas/context/context-bundle.schema.json
    """

    PLATFORM = "PLATFORM"
    UNTRUSTED = "UNTRUSTED"


class ExecutionResultStatus(str, Enum):
    """
    Generated from schemas/execution/execution-result.schema.json.

    Source: schemas/execution/execution-result.schema.json
    """

    COMPLETED = "COMPLETED"
    TIMED_OUT = "TIMED_OUT"
    CANCELLED = "CANCELLED"
    RESOURCE_EXCEEDED = "RESOURCE_EXCEEDED"
    EXECUTOR_ERROR = "EXECUTOR_ERROR"


class FailureCategory(str, Enum):
    """
    ADR-0008 §11, extended by ADR-0009. Retry is driven by category, never by message text.

    Source: schemas/common/common.schema.json
    """

    TRANSIENT = "TRANSIENT"
    MODEL_ERROR = "MODEL_ERROR"
    BUDGET_EXCEEDED = "BUDGET_EXCEEDED"
    INVALID_OUTPUT = "INVALID_OUTPUT"
    TOOL_DENIED = "TOOL_DENIED"
    VERIFICATION_FAILED = "VERIFICATION_FAILED"
    EXECUTION_ERROR = "EXECUTION_ERROR"
    INTEGRATION_ERROR = "INTEGRATION_ERROR"
    INDETERMINATE = "INDETERMINATE"
    CANCELLED = "CANCELLED"
    APPROVAL_REJECTED = "APPROVAL_REJECTED"
    APPROVAL_EXPIRED = "APPROVAL_EXPIRED"
    DEFINITION_UNAVAILABLE = "DEFINITION_UNAVAILABLE"


class GraphApprovalNodeRequiredKind(str, Enum):
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    LOCAL_OPERATOR_CONSENT = "LOCAL_OPERATOR_CONSENT"
    EXTERNAL_ATTESTATION = "EXTERNAL_ATTESTATION"


class GraphNodeOutcome(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/graph/graph.schema.json
    """

    SUCCEEDED = "SUCCEEDED"
    FAILED = "FAILED"


class GraphNodeRequiredKind(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/graph/graph.schema.json
    """

    LOCAL_OPERATOR_CONSENT = "LOCAL_OPERATOR_CONSENT"
    EXTERNAL_ATTESTATION = "EXTERNAL_ATTESTATION"


class GraphNodeType(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/graph/graph.schema.json
    """

    CONTEXT = "context"
    AGENT = "agent"
    TOOL = "tool"
    APPROVAL = "approval"
    DECISION = "decision"
    LOOP = "loop"
    TERMINAL = "terminal"


class GraphRetryPolicyBackoffStrategy(str, Enum):
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    FIXED = "FIXED"
    EXPONENTIAL = "EXPONENTIAL"


class GraphRetryPolicyRetryableCategoriesItem(str, Enum):
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    TRANSIENT = "TRANSIENT"
    MODEL_ERROR = "MODEL_ERROR"
    BUDGET_EXCEEDED = "BUDGET_EXCEEDED"
    INVALID_OUTPUT = "INVALID_OUTPUT"
    TOOL_DENIED = "TOOL_DENIED"
    VERIFICATION_FAILED = "VERIFICATION_FAILED"
    EXECUTION_ERROR = "EXECUTION_ERROR"
    INTEGRATION_ERROR = "INTEGRATION_ERROR"


class GraphRunStatus(str, Enum):
    """
    ADR-0008 §6. SUSPENDED is the normal consequence of closing a laptop — an expected,
    recoverable state, not a failure. Terminal states are immutable.

    Source: schemas/graph/graph-run.schema.json
    """

    PENDING = "PENDING"
    RUNNING = "RUNNING"
    AWAITING_APPROVAL = "AWAITING_APPROVAL"
    AWAITING_RETRY = "AWAITING_RETRY"
    SUSPENDED = "SUSPENDED"
    SUCCEEDED = "SUCCEEDED"
    FAILED = "FAILED"
    CANCELLED = "CANCELLED"
    TIMED_OUT = "TIMED_OUT"


class GraphTerminalNodeOutcome(str, Enum):
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    SUCCEEDED = "SUCCEEDED"
    FAILED = "FAILED"


class ModelRequestContentBlockType(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/model/model-request.schema.json
    """

    TEXT = "TEXT"
    ARTIFACT = "ARTIFACT"
    TOOL_CALL = "TOOL_CALL"


class ModelRequestMessagesItemRole(str, Enum):
    """
    Generated from schemas/model/model-request.schema.json.

    Source: schemas/model/model-request.schema.json
    """

    USER = "USER"
    ASSISTANT = "ASSISTANT"
    TOOL_RESULT = "TOOL_RESULT"


class ModelResponseContentItemType(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/model/model-response.schema.json
    """

    TEXT = "TEXT"
    TOOL_CALL = "TOOL_CALL"


class ModelResponseErrorCategory(str, Enum):
    """
    ADR-0010 §6. CONTENT_FILTER is recorded and never silently swallowed.

    Source: schemas/model/model-response.schema.json
    """

    RATE_LIMITED = "RATE_LIMITED"
    TRANSIENT = "TRANSIENT"
    TIMEOUT = "TIMEOUT"
    AUTH = "AUTH"
    INVALID_REQUEST = "INVALID_REQUEST"
    CONTEXT_TOO_LARGE = "CONTEXT_TOO_LARGE"
    CONTENT_FILTER = "CONTENT_FILTER"
    INVALID_OUTPUT = "INVALID_OUTPUT"
    PROVIDER_ERROR = "PROVIDER_ERROR"


class ModelResponseFinishReason(str, Enum):
    """
    Generated from schemas/model/model-response.schema.json.

    Source: schemas/model/model-response.schema.json
    """

    STOP = "STOP"
    TOOL_CALLS = "TOOL_CALLS"
    MAX_TOKENS = "MAX_TOKENS"
    CONTENT_FILTER = "CONTENT_FILTER"
    ERROR = "ERROR"


class NetworkPolicyMode(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/common/common.schema.json
    """

    NONE = "NONE"
    ALLOWLIST = "ALLOWLIST"


class NodeRunNodeType(str, Enum):
    """
    Generated from schemas/graph/node-run.schema.json.

    Source: schemas/graph/node-run.schema.json
    """

    CONTEXT = "context"
    AGENT = "agent"
    TOOL = "tool"
    APPROVAL = "approval"
    DECISION = "decision"
    LOOP = "loop"
    TERMINAL = "terminal"


class NodeRunStatus(str, Enum):
    """
    ADR-0008 §7. INDETERMINATE is entered when a write-ahead intent record exists with no
    matching outcome: a side effect may or may not have occurred. Most workflow engines omit
    this state and then discover they need it after posting two pull requests for the same
    issue.

    Source: schemas/graph/node-run.schema.json
    """

    PENDING = "PENDING"
    SCHEDULED = "SCHEDULED"
    RUNNING = "RUNNING"
    SUCCEEDED = "SUCCEEDED"
    AWAITING_APPROVAL = "AWAITING_APPROVAL"
    AWAITING_RETRY = "AWAITING_RETRY"
    FAILED = "FAILED"
    SKIPPED = "SKIPPED"
    CANCELLED = "CANCELLED"
    INDETERMINATE = "INDETERMINATE"


class NodeRunStopReason(str, Enum):
    """
    Why a loop node stopped iterating (ADR-0021 §4). Recording which bound fired is what makes
    'the loop burned the budget' diagnosable without reading logs. Present only on loop nodes;
    the iteration count is derivable from the NodeRun records and is deliberately not
    denormalised here, where it could disagree with them.

    Source: schemas/graph/node-run.schema.json
    """

    EXIT_CONDITION_MET = "EXIT_CONDITION_MET"
    MAX_ITERATIONS = "MAX_ITERATIONS"
    BODY_FAILED = "BODY_FAILED"
    BUDGET_EXCEEDED = "BUDGET_EXCEEDED"
    CANCELLED = "CANCELLED"
    DEADLINE_EXCEEDED = "DEADLINE_EXCEEDED"


class RepositoryRecordStatus(str, Enum):
    """
    Generated from schemas/registry/repository-record.schema.json.

    Source: schemas/registry/repository-record.schema.json
    """

    ACTIVE = "ACTIVE"
    SUSPENDED = "SUSPENDED"


class SideEffectClass(str, Enum):
    """
    ADR-0005 §3. Ordered from least to most consequential; a grant may narrow, never widen.

    Source: schemas/common/common.schema.json
    """

    READ = "READ"
    WORKSPACE_WRITE = "WORKSPACE_WRITE"
    EXTERNAL_WRITE = "EXTERNAL_WRITE"
    IRREVERSIBLE = "IRREVERSIBLE"


class ToolApprovalPolicyMode(str, Enum):
    """
    Discriminates the branches of the union. The members are the union of the values each branch
    permits.

    Source: schemas/tools/tool.schema.json
    """

    NEVER = "NEVER"
    ALWAYS = "ALWAYS"
    CONDITIONAL = "CONDITIONAL"


class ToolCostHint(str, Enum):
    """
    Generated from schemas/tools/tool.schema.json.

    Source: schemas/tools/tool.schema.json
    """

    LOW = "LOW"
    MEDIUM = "MEDIUM"
    HIGH = "HIGH"


class ToolIdempotency(str, Enum):
    """
    NATURAL: the external system deduplicates, or a natural key exists. KEYED: requires a
    platform-derived idempotency key. NONE: only permissible for READ.

    Source: schemas/tools/tool.schema.json
    """

    NATURAL = "NATURAL"
    KEYED = "KEYED"
    NONE = "NONE"


class ToolResultDenialReason(str, Enum):
    """
    Generated from schemas/tools/tool-result.schema.json.

    Source: schemas/tools/tool-result.schema.json
    """

    TOOL_NOT_GRANTED = "TOOL_NOT_GRANTED"
    REPOSITORY_OUT_OF_SCOPE = "REPOSITORY_OUT_OF_SCOPE"
    PATH_OUT_OF_SCOPE = "PATH_OUT_OF_SCOPE"
    SIDE_EFFECT_EXCEEDS_CEILING = "SIDE_EFFECT_EXCEEDS_CEILING"
    INPUT_VALIDATION_FAILED = "INPUT_VALIDATION_FAILED"
    BUDGET_EXCEEDED = "BUDGET_EXCEEDED"
    GRANT_EXPIRED = "GRANT_EXPIRED"


class ToolResultStatus(str, Enum):
    """
    COMPLETED: executed, output present. DENIED: authorization refused; the reason is
    model-facing so the agent can adapt rather than retry blindly. AWAITING_APPROVAL: a human
    decision is required and the node attempt is suspended (ADR-0009). FAILED: attempted and
    failed. INDETERMINATE: the platform cannot establish whether the external effect occurred —
    distinct from FAILED because a blind retry of an EXTERNAL_WRITE could duplicate it (ADR-0005
    §3).

    Source: schemas/tools/tool-result.schema.json
    """

    COMPLETED = "COMPLETED"
    DENIED = "DENIED"
    AWAITING_APPROVAL = "AWAITING_APPROVAL"
    FAILED = "FAILED"
    INDETERMINATE = "INDETERMINATE"


class TrustLevel(str, Enum):
    """
    ADR-0014 §4.

    Source: schemas/common/common.schema.json
    """

    INTERNAL = "INTERNAL"
    RESTRICTED = "RESTRICTED"
    EXTERNAL = "EXTERNAL"


class WorkspaceExecutionMode(str, Enum):
    """
    container is the default and the only mode with the isolation guarantees of ADR-0006 §4.
    unsafe-host-exec is an escape hatch that is disabled by default, requires a configuration
    change rather than a run flag, and is recorded here and on every audit entry so evaluation
    results produced under it can be excluded from comparison.

    Source: schemas/execution/workspace.schema.json
    """

    CONTAINER = "container"
    UNSAFE_HOST_EXEC = "unsafe-host-exec"


class WorkspaceStatus(str, Enum):
    """
    CREATING -> READY -> LEASED, then RELEASED -> DESTROYED on success, QUARANTINED -> DESTROYED
    on a failed run with bounded retention, or EXPIRED when the lease is lost and the workspace
    becomes reclaimable. QUARANTINED is a distinct state rather than delayed destruction because
    a workspace kept for diagnosis must not be handed to another run.

    Source: schemas/execution/workspace.schema.json
    """

    CREATING = "CREATING"
    READY = "READY"
    LEASED = "LEASED"
    RELEASED = "RELEASED"
    EXPIRED = "EXPIRED"
    QUARANTINED = "QUARANTINED"
    DESTROYED = "DESTROYED"


@dataclass(frozen=True)
class Agent:
    """
    An immutable, content-addressed agent definition (ADR-0022 §1). An Agent is declarative
    data, never code. Editing one produces a new version rather than mutating the existing
    definition, because a run pins a version for its entire life and an in-place edit would
    silently change the behaviour of work already in flight. The definition deliberately omits
    the objective and the output schema: those are per-node, live in the Graph, and are already
    pinned by graphVersionHash (ADR-0022 §2). An agent is reusable behaviour; a node is one
    specific use of it.

    Source: schemas/agent/agent.schema.json
    """

    schemaVersion: str
    agentId: str
    title: str
    description: str
    systemInstructions: list[str]
    defaultModelPolicy: AgentDefaultModelPolicy
    defaultBudgets: AgentDefaultBudgets
    capabilityCeiling: AgentCapabilityCeiling
    outputContractStyle: AgentOutputContractStyle
    agentVersionHash: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["agentId"] = self.agentId
        data["title"] = self.title
        data["description"] = self.description
        data["systemInstructions"] = list(self.systemInstructions)
        data["defaultModelPolicy"] = self.defaultModelPolicy.to_dict()
        data["defaultBudgets"] = self.defaultBudgets.to_dict()
        data["capabilityCeiling"] = self.capabilityCeiling.to_dict()
        data["outputContractStyle"] = self.outputContractStyle.value
        if self.agentVersionHash is not None:
            data["agentVersionHash"] = self.agentVersionHash
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> Agent:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            agentId=data["agentId"],
            title=data["title"],
            description=data["description"],
            systemInstructions=list(data["systemInstructions"]),
            defaultModelPolicy=AgentDefaultModelPolicy.from_dict(data["defaultModelPolicy"]),
            defaultBudgets=AgentDefaultBudgets.from_dict(data["defaultBudgets"]),
            capabilityCeiling=AgentCapabilityCeiling.from_dict(data["capabilityCeiling"]),
            outputContractStyle=AgentOutputContractStyle(data["outputContractStyle"]),
            agentVersionHash=None if data.get("agentVersionHash") is None else data["agentVersionHash"],
        )


@dataclass(frozen=True)
class AgentCapabilityCeiling:
    """
    The most this agent may ever be granted, extending the derivation chain of ADR-0014 §5:
    platform policy contains trust-level ceiling contains registry record contains agent ceiling
    contains node request. Monotonically non-increasing at every step. Its value is that a
    general-purpose agent can be denied IRREVERSIBLE tools once, centrally, instead of in every
    node that uses it. Validated against platform policy at publication (ADR-0022 §6), so an
    over-broad agent cannot be published and then discovered in production.

    Source: schemas/agent/agent.schema.json
    """

    allowedTools: list[str]
    maxSideEffect: SideEffectClass
    commandProfiles: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["allowedTools"] = list(self.allowedTools)
        data["maxSideEffect"] = self.maxSideEffect.value
        if self.commandProfiles is not None:
            data["commandProfiles"] = list(self.commandProfiles)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentCapabilityCeiling:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            allowedTools=list(data["allowedTools"]),
            maxSideEffect=SideEffectClass(data["maxSideEffect"]),
            commandProfiles=None if data.get("commandProfiles") is None else list(data["commandProfiles"]),
        )


@dataclass(frozen=True)
class AgentDefaultBudgets:
    """
    Defaults for the agent loop. maxIterations bounds the agent's internal reason-act loop
    within one node attempt — it is not the graph loop bound of ADR-0021 §4, which is a property
    of a loop node.

    Source: schemas/agent/agent.schema.json
    """

    maxIterations: int
    maxTokens: int
    maxCostUnits: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["maxIterations"] = self.maxIterations
        data["maxTokens"] = self.maxTokens
        if self.maxCostUnits is not None:
            data["maxCostUnits"] = self.maxCostUnits
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentDefaultBudgets:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxIterations=data["maxIterations"],
            maxTokens=data["maxTokens"],
            maxCostUnits=None if data.get("maxCostUnits") is None else data["maxCostUnits"],
        )


@dataclass(frozen=True)
class AgentDefaultModelPolicy:
    """
    The agent's default model. A node may override it. Expressed as either a capability selector
    or a concrete pinned reference; the selector form exists so an agent definition need not be
    re-published when a provider retires a model version. Union of mutually exclusive branches;
    exactly one branch's required fields are present in a valid document, which the JSON Schema
    enforces and this type does not.

    Source: schemas/agent/agent.schema.json
    """

    selector: str | None = None
    modelRef: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.selector is not None:
            data["selector"] = self.selector
        if self.modelRef is not None:
            data["modelRef"] = self.modelRef
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentDefaultModelPolicy:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            selector=None if data.get("selector") is None else data["selector"],
            modelRef=None if data.get("modelRef") is None else data["modelRef"],
        )


@dataclass(frozen=True)
class AgentExecutionRequest:
    """
    POST /v1/agent-executions, control plane to agent runtime (ADR-0004 §11). One call, exactly
    one node attempt. The runtime never selects the next node, never decides to retry, never
    branches and never schedules work; all workflow control lives in the graph engine.

    Source: schemas/agent/agent-execution-request.schema.json
    """

    protocolVersion: str
    runId: str
    nodeId: str
    iteration: int
    attempt: int
    agentId: str
    agentVersionHash: str
    objective: str
    instructions: list[str]
    contextBundleRef: str
    tools: list[Tool]
    outputSchema: dict[str, Any]
    budgets: AgentExecutionRequestBudgets
    callbackToken: str
    traceparent: str | None = None
    modelPolicy: AgentExecutionRequestModelPolicy | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["runId"] = self.runId
        data["nodeId"] = self.nodeId
        data["iteration"] = self.iteration
        data["attempt"] = self.attempt
        data["agentId"] = self.agentId
        data["agentVersionHash"] = self.agentVersionHash
        data["objective"] = self.objective
        data["instructions"] = list(self.instructions)
        data["contextBundleRef"] = self.contextBundleRef
        data["tools"] = [_i.to_dict() for _i in self.tools]
        data["outputSchema"] = self.outputSchema
        data["budgets"] = self.budgets.to_dict()
        data["callbackToken"] = self.callbackToken
        if self.traceparent is not None:
            data["traceparent"] = self.traceparent
        if self.modelPolicy is not None:
            data["modelPolicy"] = self.modelPolicy.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentExecutionRequest:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            runId=data["runId"],
            nodeId=data["nodeId"],
            iteration=data["iteration"],
            attempt=data["attempt"],
            agentId=data["agentId"],
            agentVersionHash=data["agentVersionHash"],
            objective=data["objective"],
            instructions=list(data["instructions"]),
            contextBundleRef=data["contextBundleRef"],
            tools=[Tool.from_dict(_i) for _i in data["tools"]],
            outputSchema=data["outputSchema"],
            budgets=AgentExecutionRequestBudgets.from_dict(data["budgets"]),
            callbackToken=data["callbackToken"],
            traceparent=None if data.get("traceparent") is None else data["traceparent"],
            modelPolicy=None if data.get("modelPolicy") is None else AgentExecutionRequestModelPolicy.from_dict(data["modelPolicy"]),
        )


@dataclass(frozen=True)
class AgentExecutionRequestBudgets:
    """
    Generated from schemas/agent/agent-execution-request.schema.json.

    Source: schemas/agent/agent-execution-request.schema.json
    """

    maxIterations: int
    maxTokens: int
    deadline: str
    maxCostUnits: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["maxIterations"] = self.maxIterations
        data["maxTokens"] = self.maxTokens
        data["deadline"] = self.deadline
        if self.maxCostUnits is not None:
            data["maxCostUnits"] = self.maxCostUnits
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentExecutionRequestBudgets:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxIterations=data["maxIterations"],
            maxTokens=data["maxTokens"],
            deadline=data["deadline"],
            maxCostUnits=None if data.get("maxCostUnits") is None else data["maxCostUnits"],
        )


@dataclass(frozen=True)
class AgentExecutionRequestModelPolicy:
    """
    Generated from schemas/agent/agent-execution-request.schema.json.

    Source: schemas/agent/agent-execution-request.schema.json
    """

    selector: str | None = None
    modelRef: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.selector is not None:
            data["selector"] = self.selector
        if self.modelRef is not None:
            data["modelRef"] = self.modelRef
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentExecutionRequestModelPolicy:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            selector=None if data.get("selector") is None else data["selector"],
            modelRef=None if data.get("modelRef") is None else data["modelRef"],
        )


@dataclass(frozen=True)
class AgentExecutionResult:
    """
    Returned by the agent runtime for one node attempt (ADR-0004 §11). ADR-0004 §9: the control
    plane never trusts this self-report. claimedOutput is validated against the node's declared
    output schema before any transition is evaluated; usage and cost are taken from the model
    gateway's own records rather than from the runtime's summary; tool effects are known from
    the tool layer's records rather than from the runtime's narrative. That is what makes this a
    trust boundary rather than a module boundary.

    Source: schemas/agent/agent-execution-result.schema.json
    """

    protocolVersion: str
    status: AgentExecutionResultStatus
    agentLoopIterations: int
    claimedOutput: dict[str, Any] | None = None
    stopReason: str | None = None
    failure: AgentExecutionResultFailure | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["status"] = self.status.value
        data["agentLoopIterations"] = self.agentLoopIterations
        if self.claimedOutput is not None:
            data["claimedOutput"] = self.claimedOutput
        if self.stopReason is not None:
            data["stopReason"] = self.stopReason
        if self.failure is not None:
            data["failure"] = self.failure.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentExecutionResult:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            status=AgentExecutionResultStatus(data["status"]),
            agentLoopIterations=data["agentLoopIterations"],
            claimedOutput=None if data.get("claimedOutput") is None else data["claimedOutput"],
            stopReason=None if data.get("stopReason") is None else data["stopReason"],
            failure=None if data.get("failure") is None else AgentExecutionResultFailure.from_dict(data["failure"]),
        )


@dataclass(frozen=True)
class AgentExecutionResultFailure:
    """
    Generated from schemas/agent/agent-execution-result.schema.json.

    Source: schemas/agent/agent-execution-result.schema.json
    """

    category: FailureCategory
    message: str
    detailsRef: ArtifactRef | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["category"] = self.category.value
        data["message"] = self.message
        if self.detailsRef is not None:
            data["detailsRef"] = self.detailsRef.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AgentExecutionResultFailure:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            category=FailureCategory(data["category"]),
            message=data["message"],
            detailsRef=None if data.get("detailsRef") is None else ArtifactRef.from_dict(data["detailsRef"]),
        )


@dataclass(frozen=True)
class Approval:
    """
    A human decision on a consequential action (ADR-0009). The record binds to subjectHash — the
    content hash of exactly what was approved — and the engine re-verifies it on resume. Without
    that binding an approval is a time-of-check/time-of-use bug with a human in it.

    Source: schemas/approval/approval.schema.json
    """

    schemaVersion: str
    approvalId: str
    runId: str
    nodeId: str
    kind: ApprovalKind
    requiredKind: ApprovalRequiredKind
    subjectHash: str
    subjectRef: ArtifactRef
    renderingHash: str
    status: ApprovalStatus
    expiresAt: str
    interactive: bool
    createdAt: str
    decidedBy: str | None = None
    decidedAt: str | None = None
    externalRef: str | None = None
    rationale: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["approvalId"] = self.approvalId
        data["runId"] = self.runId
        data["nodeId"] = self.nodeId
        data["kind"] = self.kind.value
        data["requiredKind"] = self.requiredKind.value
        data["subjectHash"] = self.subjectHash
        data["subjectRef"] = self.subjectRef.to_dict()
        data["renderingHash"] = self.renderingHash
        data["status"] = self.status.value
        data["expiresAt"] = self.expiresAt
        data["interactive"] = self.interactive
        data["createdAt"] = self.createdAt
        if self.decidedBy is not None:
            data["decidedBy"] = self.decidedBy
        if self.decidedAt is not None:
            data["decidedAt"] = self.decidedAt
        if self.externalRef is not None:
            data["externalRef"] = self.externalRef
        if self.rationale is not None:
            data["rationale"] = self.rationale
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> Approval:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            approvalId=data["approvalId"],
            runId=data["runId"],
            nodeId=data["nodeId"],
            kind=ApprovalKind(data["kind"]),
            requiredKind=ApprovalRequiredKind(data["requiredKind"]),
            subjectHash=data["subjectHash"],
            subjectRef=ArtifactRef.from_dict(data["subjectRef"]),
            renderingHash=data["renderingHash"],
            status=ApprovalStatus(data["status"]),
            expiresAt=data["expiresAt"],
            interactive=data["interactive"],
            createdAt=data["createdAt"],
            decidedBy=None if data.get("decidedBy") is None else data["decidedBy"],
            decidedAt=None if data.get("decidedAt") is None else data["decidedAt"],
            externalRef=None if data.get("externalRef") is None else data["externalRef"],
            rationale=None if data.get("rationale") is None else data["rationale"],
        )


@dataclass(frozen=True)
class ArtifactRef:
    """
    A reference to content in the artifact store (ADR-0017 §2). ADR-0003 §3 requires that
    artifacts cross the execution boundary as references and never as local paths or open
    handles, so this type appears in most other contracts.

    Source: schemas/artifacts/artifact-ref.schema.json
    """

    uri: str
    sha256: str
    sizeBytes: int
    contentType: str
    classification: ArtifactRefClassification
    createdAt: str
    encoding: ArtifactRefEncoding | None = None
    expiresAt: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["uri"] = self.uri
        data["sha256"] = self.sha256
        data["sizeBytes"] = self.sizeBytes
        data["contentType"] = self.contentType
        data["classification"] = self.classification.to_dict()
        data["createdAt"] = self.createdAt
        if self.encoding is not None:
            data["encoding"] = self.encoding.value
        if self.expiresAt is not None:
            data["expiresAt"] = self.expiresAt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ArtifactRef:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            uri=data["uri"],
            sha256=data["sha256"],
            sizeBytes=data["sizeBytes"],
            contentType=data["contentType"],
            classification=ArtifactRefClassification.from_dict(data["classification"]),
            createdAt=data["createdAt"],
            encoding=None if data.get("encoding") is None else ArtifactRefEncoding(data["encoding"]),
            expiresAt=None if data.get("expiresAt") is None else data["expiresAt"],
        )


@dataclass(frozen=True)
class ArtifactRefClassification:
    """
    ADR-0017 §1. Every artifact is classified at write time; classification is not inferred
    later.

    Source: schemas/artifacts/artifact-ref.schema.json
    """

    artifactType: ArtifactRefClassificationArtifactType
    sensitivity: ArtifactRefClassificationSensitivity
    provenance: ArtifactRefClassificationProvenance
    redacted: bool
    retentionTier: ArtifactRefClassificationRetentionTier

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["artifactType"] = self.artifactType.value
        data["sensitivity"] = self.sensitivity.value
        data["provenance"] = self.provenance.value
        data["redacted"] = self.redacted
        data["retentionTier"] = self.retentionTier.value
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ArtifactRefClassification:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            artifactType=ArtifactRefClassificationArtifactType(data["artifactType"]),
            sensitivity=ArtifactRefClassificationSensitivity(data["sensitivity"]),
            provenance=ArtifactRefClassificationProvenance(data["provenance"]),
            redacted=data["redacted"],
            retentionTier=ArtifactRefClassificationRetentionTier(data["retentionTier"]),
        )


@dataclass(frozen=True)
class AuditEntry:
    """
    An append-only record of an attempted consequential action (ADR-0016). Written fail-closed:
    if the entry cannot be persisted, the action does not proceed. Retained independently of
    telemetry — traces may be sampled and logs may be dropped, audit may not. ADR-0016 is
    explicit about the limits of what V1 can attest to; see the assurance field.

    Source: schemas/audit/audit-entry.schema.json
    """

    schemaVersion: str
    auditId: str
    sequence: int
    timestamp: str
    runId: str
    action: str
    decision: AuditEntryDecision
    assurance: AuditEntryAssurance
    actor: AuditEntryActor
    prevHash: str
    entryHash: str
    nodeId: str | None = None
    attempt: int | None = None
    iteration: int | None = None
    graphId: str | None = None
    graphVersionHash: str | None = None
    agentId: str | None = None
    subjectRef: str | None = None
    subjectHash: str | None = None
    sideEffectClass: SideEffectClass | None = None
    capabilityGrantId: str | None = None
    reason: str | None = None
    externalRef: str | None = None
    traceId: str | None = None
    spanId: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["auditId"] = self.auditId
        data["sequence"] = self.sequence
        data["timestamp"] = self.timestamp
        data["runId"] = self.runId
        data["action"] = self.action
        data["decision"] = self.decision.value
        data["assurance"] = self.assurance.value
        data["actor"] = self.actor.to_dict()
        data["prevHash"] = self.prevHash
        data["entryHash"] = self.entryHash
        if self.nodeId is not None:
            data["nodeId"] = self.nodeId
        if self.attempt is not None:
            data["attempt"] = self.attempt
        if self.iteration is not None:
            data["iteration"] = self.iteration
        if self.graphId is not None:
            data["graphId"] = self.graphId
        if self.graphVersionHash is not None:
            data["graphVersionHash"] = self.graphVersionHash
        if self.agentId is not None:
            data["agentId"] = self.agentId
        if self.subjectRef is not None:
            data["subjectRef"] = self.subjectRef
        if self.subjectHash is not None:
            data["subjectHash"] = self.subjectHash
        if self.sideEffectClass is not None:
            data["sideEffectClass"] = self.sideEffectClass.value
        if self.capabilityGrantId is not None:
            data["capabilityGrantId"] = self.capabilityGrantId
        if self.reason is not None:
            data["reason"] = self.reason
        if self.externalRef is not None:
            data["externalRef"] = self.externalRef
        if self.traceId is not None:
            data["traceId"] = self.traceId
        if self.spanId is not None:
            data["spanId"] = self.spanId
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AuditEntry:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            auditId=data["auditId"],
            sequence=data["sequence"],
            timestamp=data["timestamp"],
            runId=data["runId"],
            action=data["action"],
            decision=AuditEntryDecision(data["decision"]),
            assurance=AuditEntryAssurance(data["assurance"]),
            actor=AuditEntryActor.from_dict(data["actor"]),
            prevHash=data["prevHash"],
            entryHash=data["entryHash"],
            nodeId=None if data.get("nodeId") is None else data["nodeId"],
            attempt=None if data.get("attempt") is None else data["attempt"],
            iteration=None if data.get("iteration") is None else data["iteration"],
            graphId=None if data.get("graphId") is None else data["graphId"],
            graphVersionHash=None if data.get("graphVersionHash") is None else data["graphVersionHash"],
            agentId=None if data.get("agentId") is None else data["agentId"],
            subjectRef=None if data.get("subjectRef") is None else data["subjectRef"],
            subjectHash=None if data.get("subjectHash") is None else data["subjectHash"],
            sideEffectClass=None if data.get("sideEffectClass") is None else SideEffectClass(data["sideEffectClass"]),
            capabilityGrantId=None if data.get("capabilityGrantId") is None else data["capabilityGrantId"],
            reason=None if data.get("reason") is None else data["reason"],
            externalRef=None if data.get("externalRef") is None else data["externalRef"],
            traceId=None if data.get("traceId") is None else data["traceId"],
            spanId=None if data.get("spanId") is None else data["spanId"],
        )


@dataclass(frozen=True)
class AuditEntryActor:
    """
    Generated from schemas/audit/audit-entry.schema.json.

    Source: schemas/audit/audit-entry.schema.json
    """

    localUser: str
    platformInstanceId: str
    initiatedBy: str
    externalIdentity: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["localUser"] = self.localUser
        data["platformInstanceId"] = self.platformInstanceId
        data["initiatedBy"] = self.initiatedBy
        if self.externalIdentity is not None:
            data["externalIdentity"] = self.externalIdentity
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> AuditEntryActor:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            localUser=data["localUser"],
            platformInstanceId=data["platformInstanceId"],
            initiatedBy=data["initiatedBy"],
            externalIdentity=None if data.get("externalIdentity") is None else data["externalIdentity"],
        )


@dataclass(frozen=True)
class CapabilityGrant:
    """
    Minted by the control plane per node attempt (ADR-0005 §4). Derived from the graph node
    definition intersected with the repository registry's capability ceiling, never from
    anything the model produced. Monotonically non-increasing: no operation grows a grant
    mid-attempt, and a retry receives a fresh grant so a failed attempt leaves no residual
    authority. The agent runtime never handles a grant, only the attempt token that resolves to
    it.

    Source: schemas/tools/capability-grant.schema.json
    """

    schemaVersion: str
    grantId: str
    subject: CapabilityGrantSubject
    allowedTools: list[CapabilityGrantAllowedToolsItem]
    maxSideEffect: SideEffectClass
    repositoryScope: list[str]
    pathScope: dict[str, PathScopeEntry]
    budget: CapabilityGrantBudget
    notAfter: str
    externalScope: CapabilityGrantExternalScope | None = None
    commandProfiles: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["grantId"] = self.grantId
        data["subject"] = self.subject.to_dict()
        data["allowedTools"] = [_i.to_dict() for _i in self.allowedTools]
        data["maxSideEffect"] = self.maxSideEffect.value
        data["repositoryScope"] = list(self.repositoryScope)
        data["pathScope"] = {_k: _v.to_dict() for _k, _v in self.pathScope.items()}
        data["budget"] = self.budget.to_dict()
        data["notAfter"] = self.notAfter
        if self.externalScope is not None:
            data["externalScope"] = self.externalScope.to_dict()
        if self.commandProfiles is not None:
            data["commandProfiles"] = list(self.commandProfiles)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CapabilityGrant:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            grantId=data["grantId"],
            subject=CapabilityGrantSubject.from_dict(data["subject"]),
            allowedTools=[CapabilityGrantAllowedToolsItem.from_dict(_i) for _i in data["allowedTools"]],
            maxSideEffect=SideEffectClass(data["maxSideEffect"]),
            repositoryScope=list(data["repositoryScope"]),
            pathScope={_k: PathScopeEntry.from_dict(_v) for _k, _v in data["pathScope"].items()},
            budget=CapabilityGrantBudget.from_dict(data["budget"]),
            notAfter=data["notAfter"],
            externalScope=None if data.get("externalScope") is None else CapabilityGrantExternalScope.from_dict(data["externalScope"]),
            commandProfiles=None if data.get("commandProfiles") is None else list(data["commandProfiles"]),
        )


@dataclass(frozen=True)
class CapabilityGrantAllowedToolsItem:
    """
    Generated from schemas/tools/capability-grant.schema.json.

    Source: schemas/tools/capability-grant.schema.json
    """

    id_: str
    version: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["id"] = self.id_
        data["version"] = self.version
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CapabilityGrantAllowedToolsItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=data["id"],
            version=data["version"],
        )


@dataclass(frozen=True)
class CapabilityGrantBudget:
    """
    Generated from schemas/tools/capability-grant.schema.json.

    Source: schemas/tools/capability-grant.schema.json
    """

    maxInvocations: int
    maxWallClockMs: int
    maxDenials: int
    maxCostUnits: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["maxInvocations"] = self.maxInvocations
        data["maxWallClockMs"] = self.maxWallClockMs
        data["maxDenials"] = self.maxDenials
        if self.maxCostUnits is not None:
            data["maxCostUnits"] = self.maxCostUnits
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CapabilityGrantBudget:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxInvocations=data["maxInvocations"],
            maxWallClockMs=data["maxWallClockMs"],
            maxDenials=data["maxDenials"],
            maxCostUnits=None if data.get("maxCostUnits") is None else data["maxCostUnits"],
        )


@dataclass(frozen=True)
class CapabilityGrantExternalScope:
    """
    Generated from schemas/tools/capability-grant.schema.json.

    Source: schemas/tools/capability-grant.schema.json
    """

    jiraProjectKeys: list[str] | None = None
    githubRepositories: list[str] | None = None
    confluenceSpaceKeys: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.jiraProjectKeys is not None:
            data["jiraProjectKeys"] = list(self.jiraProjectKeys)
        if self.githubRepositories is not None:
            data["githubRepositories"] = list(self.githubRepositories)
        if self.confluenceSpaceKeys is not None:
            data["confluenceSpaceKeys"] = list(self.confluenceSpaceKeys)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CapabilityGrantExternalScope:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            jiraProjectKeys=None if data.get("jiraProjectKeys") is None else list(data["jiraProjectKeys"]),
            githubRepositories=None if data.get("githubRepositories") is None else list(data["githubRepositories"]),
            confluenceSpaceKeys=None if data.get("confluenceSpaceKeys") is None else list(data["confluenceSpaceKeys"]),
        )


@dataclass(frozen=True)
class CapabilityGrantSubject:
    """
    The NodeRun attempt this grant authorises, using the four-part key of ADR-0021 §1. iteration
    is required: without it, two attempts in different loop rounds would share a subject.

    Source: schemas/tools/capability-grant.schema.json
    """

    runId: str
    nodeId: str
    iteration: int
    attempt: int

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["runId"] = self.runId
        data["nodeId"] = self.nodeId
        data["iteration"] = self.iteration
        data["attempt"] = self.attempt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CapabilityGrantSubject:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            runId=data["runId"],
            nodeId=data["nodeId"],
            iteration=data["iteration"],
            attempt=data["attempt"],
        )


@dataclass(frozen=True)
class Checkpoint:
    """
    Written at every node boundary, in the same transactional batch as the node's terminal
    status (ADR-0008 §10). Intra-agent iteration checkpoints are explicitly out of scope: an
    agent attempt is the unit of retry, and a partially completed agent loop is re-run, not
    resumed.

    Source: schemas/graph/checkpoint.schema.json
    """

    schemaVersion: str
    checkpointId: str
    runId: str
    afterNodeId: str
    afterIteration: int
    afterAttempt: int
    sequence: int
    workspaceStateRef: CheckpointWorkspaceState
    createdAt: str
    graphVariablesRef: ArtifactRef | None = None
    contextBundleRefs: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["checkpointId"] = self.checkpointId
        data["runId"] = self.runId
        data["afterNodeId"] = self.afterNodeId
        data["afterIteration"] = self.afterIteration
        data["afterAttempt"] = self.afterAttempt
        data["sequence"] = self.sequence
        data["workspaceStateRef"] = self.workspaceStateRef.to_dict()
        data["createdAt"] = self.createdAt
        if self.graphVariablesRef is not None:
            data["graphVariablesRef"] = self.graphVariablesRef.to_dict()
        if self.contextBundleRefs is not None:
            data["contextBundleRefs"] = list(self.contextBundleRefs)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> Checkpoint:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            checkpointId=data["checkpointId"],
            runId=data["runId"],
            afterNodeId=data["afterNodeId"],
            afterIteration=data["afterIteration"],
            afterAttempt=data["afterAttempt"],
            sequence=data["sequence"],
            workspaceStateRef=CheckpointWorkspaceState.from_dict(data["workspaceStateRef"]),
            createdAt=data["createdAt"],
            graphVariablesRef=None if data.get("graphVariablesRef") is None else ArtifactRef.from_dict(data["graphVariablesRef"]),
            contextBundleRefs=None if data.get("contextBundleRefs") is None else list(data["contextBundleRefs"]),
        )


@dataclass(frozen=True)
class CheckpointWorkspaceState:
    """
    The load-bearing invariant of ADR-0003 §6: workspace state is at all times a function of
    (repoRef, baseSHA, ordered patch series), all of which live in durable state. This is
    simultaneously what makes resume work and what makes a remote executor a drop-in. Without
    it, workspaces become precious mutable state on one laptop and both properties are lost.

    Source: schemas/graph/checkpoint.schema.json
    """

    repositories: list[CheckpointWorkspaceStateRepositoriesItem]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["repositories"] = [_i.to_dict() for _i in self.repositories]
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CheckpointWorkspaceState:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            repositories=[CheckpointWorkspaceStateRepositoriesItem.from_dict(_i) for _i in data["repositories"]],
        )


@dataclass(frozen=True)
class CheckpointWorkspaceStateRepositoriesItem:
    """
    Generated from schemas/graph/checkpoint.schema.json.

    Source: schemas/graph/checkpoint.schema.json
    """

    repoRef: RepoRef
    baseSha: str
    patchSeries: list[ArtifactRef]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["repoRef"] = self.repoRef.to_dict()
        data["baseSha"] = self.baseSha
        data["patchSeries"] = [_i.to_dict() for _i in self.patchSeries]
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CheckpointWorkspaceStateRepositoriesItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            repoRef=RepoRef.from_dict(data["repoRef"]),
            baseSha=data["baseSha"],
            patchSeries=[ArtifactRef.from_dict(_i) for _i in data["patchSeries"]],
        )


@dataclass(frozen=True)
class CommandProfile:
    """
    A named, argv-fixed command declared by a repository (ADR-0005 §5).
    workspace.run_profile(profileId, parameters) is the only route to command execution: there
    is no tool that accepts a shell string or a free-form argv array from a model. Parameters
    are substituted as discrete argv elements, never concatenated into a shell, and no shell is
    involved at any layer.

    Source: schemas/tools/command-profile.schema.json
    """

    schemaVersion: str
    profileId: str
    description: str
    argv: list[str]
    parameters: list[CommandProfileParametersItem]
    cwdScope: list[str]
    timeoutMs: int
    resourceLimits: ResourceLimits
    networkPolicy: NetworkPolicy
    sideEffectClass: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["profileId"] = self.profileId
        data["description"] = self.description
        data["argv"] = list(self.argv)
        data["parameters"] = [_i.to_dict() for _i in self.parameters]
        data["cwdScope"] = list(self.cwdScope)
        data["timeoutMs"] = self.timeoutMs
        data["resourceLimits"] = self.resourceLimits.to_dict()
        data["networkPolicy"] = self.networkPolicy.to_dict()
        data["sideEffectClass"] = self.sideEffectClass
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CommandProfile:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            profileId=data["profileId"],
            description=data["description"],
            argv=list(data["argv"]),
            parameters=[CommandProfileParametersItem.from_dict(_i) for _i in data["parameters"]],
            cwdScope=list(data["cwdScope"]),
            timeoutMs=data["timeoutMs"],
            resourceLimits=ResourceLimits.from_dict(data["resourceLimits"]),
            networkPolicy=NetworkPolicy.from_dict(data["networkPolicy"]),
            sideEffectClass=data["sideEffectClass"],
        )


@dataclass(frozen=True)
class CommandProfileParametersItem:
    """
    Generated from schemas/tools/command-profile.schema.json.

    Source: schemas/tools/command-profile.schema.json
    """

    name: str
    type_: CommandProfileParametersItemType
    required: bool
    pattern: str | None = None
    allowedValues: list[str] | None = None
    default: Any | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["name"] = self.name
        data["type"] = self.type_.value
        data["required"] = self.required
        if self.pattern is not None:
            data["pattern"] = self.pattern
        if self.allowedValues is not None:
            data["allowedValues"] = list(self.allowedValues)
        if self.default is not None:
            data["default"] = self.default
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> CommandProfileParametersItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            name=data["name"],
            type_=CommandProfileParametersItemType(data["type"]),
            required=data["required"],
            pattern=None if data.get("pattern") is None else data["pattern"],
            allowedValues=None if data.get("allowedValues") is None else list(data["allowedValues"]),
            default=None if data.get("default") is None else data["default"],
        )


@dataclass(frozen=True)
class ConditionComparison:
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    op: ConditionComparisonOp
    left: ConditionReference
    right: ConditionComparisonRight

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["op"] = self.op.value
        data["left"] = self.left.to_dict()
        data["right"] = self.right.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionComparison:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            op=ConditionComparisonOp(data["op"]),
            left=ConditionReference.from_dict(data["left"]),
            right=ConditionComparisonRight.from_dict(data["right"]),
        )


@dataclass(frozen=True)
class ConditionComparisonRight:
    """
    Union of mutually exclusive branches; exactly one branch's required fields are present in a
    valid document, which the JSON Schema enforces and this type does not.

    Source: schemas/graph/condition.schema.json
    """

    source: ConditionComparisonRightSource | None = None
    nodeId: str | None = None
    path: str | None = None
    value: Any | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.source is not None:
            data["source"] = self.source.value
        if self.nodeId is not None:
            data["nodeId"] = self.nodeId
        if self.path is not None:
            data["path"] = self.path
        if self.value is not None:
            data["value"] = self.value
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionComparisonRight:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            source=None if data.get("source") is None else ConditionComparisonRightSource(data["source"]),
            nodeId=None if data.get("nodeId") is None else data["nodeId"],
            path=None if data.get("path") is None else data["path"],
            value=None if data.get("value") is None else data["value"],
        )


@dataclass(frozen=True)
class ConditionComposition:
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    op: ConditionCompositionOp
    operands: list[ConditionExpression]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["op"] = self.op.value
        data["operands"] = [_i.to_dict() for _i in self.operands]
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionComposition:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            op=ConditionCompositionOp(data["op"]),
            operands=[ConditionExpression.from_dict(_i) for _i in data["operands"]],
        )


@dataclass(frozen=True)
class ConditionConstant:
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    const: bool

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["const"] = self.const
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionConstant:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            const=data["const"],
        )


@dataclass(frozen=True)
class ConditionExpression:
    """
    Union of mutually exclusive branches; exactly one branch's required fields are present in a
    valid document, which the JSON Schema enforces and this type does not.

    Source: schemas/graph/condition.schema.json
    """

    op: ConditionExpressionOp | None = None
    left: ConditionReference | None = None
    right: ConditionExpressionRight | None = None
    values: list[Any] | None = None
    operands: list[ConditionExpression] | None = None
    operand: ConditionExpression | None = None
    const: bool | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.op is not None:
            data["op"] = self.op.value
        if self.left is not None:
            data["left"] = self.left.to_dict()
        if self.right is not None:
            data["right"] = self.right.to_dict()
        if self.values is not None:
            data["values"] = list(self.values)
        if self.operands is not None:
            data["operands"] = [_i.to_dict() for _i in self.operands]
        if self.operand is not None:
            data["operand"] = self.operand.to_dict()
        if self.const is not None:
            data["const"] = self.const
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionExpression:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            op=None if data.get("op") is None else ConditionExpressionOp(data["op"]),
            left=None if data.get("left") is None else ConditionReference.from_dict(data["left"]),
            right=None if data.get("right") is None else ConditionExpressionRight.from_dict(data["right"]),
            values=None if data.get("values") is None else list(data["values"]),
            operands=None if data.get("operands") is None else [ConditionExpression.from_dict(_i) for _i in data["operands"]],
            operand=None if data.get("operand") is None else ConditionExpression.from_dict(data["operand"]),
            const=None if data.get("const") is None else data["const"],
        )


@dataclass(frozen=True)
class ConditionExpressionRight:
    """
    Union of mutually exclusive branches; exactly one branch's required fields are present in a
    valid document, which the JSON Schema enforces and this type does not.

    Source: schemas/graph/condition.schema.json
    """

    source: ConditionExpressionRightSource | None = None
    nodeId: str | None = None
    path: str | None = None
    value: Any | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.source is not None:
            data["source"] = self.source.value
        if self.nodeId is not None:
            data["nodeId"] = self.nodeId
        if self.path is not None:
            data["path"] = self.path
        if self.value is not None:
            data["value"] = self.value
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionExpressionRight:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            source=None if data.get("source") is None else ConditionExpressionRightSource(data["source"]),
            nodeId=None if data.get("nodeId") is None else data["nodeId"],
            path=None if data.get("path") is None else data["path"],
            value=None if data.get("value") is None else data["value"],
        )


@dataclass(frozen=True)
class ConditionMembership:
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    op: ConditionMembershipOp
    left: ConditionReference
    values: list[Any]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["op"] = self.op.value
        data["left"] = self.left.to_dict()
        data["values"] = list(self.values)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionMembership:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            op=ConditionMembershipOp(data["op"]),
            left=ConditionReference.from_dict(data["left"]),
            values=list(data["values"]),
        )


@dataclass(frozen=True)
class ConditionNegation:
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    op: str
    operand: ConditionExpression

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["op"] = self.op
        data["operand"] = self.operand.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionNegation:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            op=data["op"],
            operand=ConditionExpression.from_dict(data["operand"]),
        )


@dataclass(frozen=True)
class ConditionPresence:
    """
    Generated from schemas/graph/condition.schema.json.

    Source: schemas/graph/condition.schema.json
    """

    op: ConditionPresenceOp
    left: ConditionReference

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["op"] = self.op.value
        data["left"] = self.left.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionPresence:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            op=ConditionPresenceOp(data["op"]),
            left=ConditionReference.from_dict(data["left"]),
        )


@dataclass(frozen=True)
class ConditionReference:
    """
    A path into validated structured node output or into run variables. Nothing else is
    addressable.

    Source: schemas/graph/condition.schema.json
    """

    source: ConditionReferenceSource
    path: str
    nodeId: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["source"] = self.source.value
        data["path"] = self.path
        if self.nodeId is not None:
            data["nodeId"] = self.nodeId
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ConditionReference:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            source=ConditionReferenceSource(data["source"]),
            path=data["path"],
            nodeId=None if data.get("nodeId") is None else data["nodeId"],
        )


@dataclass(frozen=True)
class ContextBundle:
    """
    An immutable, content-addressed set of retrieved material assembled for one node attempt
    (ADR-0013). bundleRef is sha256 of the canonical serialisation. The bundle records not only
    what was included but why, what was excluded and under whose identity each item was
    retrieved, so the platform can answer 'why did the agent see this?' from durable state.

    Source: schemas/context/context-bundle.schema.json
    """

    schemaVersion: str
    bundleRef: str
    runId: str
    nodeId: str
    createdAt: str
    budget: ContextBundleBudget
    pins: ContextBundlePins
    items: list[ContextBundleItem]
    excluded: list[ContextBundleExcludedItem]
    supersedes: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["bundleRef"] = self.bundleRef
        data["runId"] = self.runId
        data["nodeId"] = self.nodeId
        data["createdAt"] = self.createdAt
        data["budget"] = self.budget.to_dict()
        data["pins"] = self.pins.to_dict()
        data["items"] = [_i.to_dict() for _i in self.items]
        data["excluded"] = [_i.to_dict() for _i in self.excluded]
        if self.supersedes is not None:
            data["supersedes"] = self.supersedes
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ContextBundle:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            bundleRef=data["bundleRef"],
            runId=data["runId"],
            nodeId=data["nodeId"],
            createdAt=data["createdAt"],
            budget=ContextBundleBudget.from_dict(data["budget"]),
            pins=ContextBundlePins.from_dict(data["pins"]),
            items=[ContextBundleItem.from_dict(_i) for _i in data["items"]],
            excluded=[ContextBundleExcludedItem.from_dict(_i) for _i in data["excluded"]],
            supersedes=None if data.get("supersedes") is None else data["supersedes"],
        )


@dataclass(frozen=True)
class ContextBundleBudget:
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    limit: int
    used: int
    policyId: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["limit"] = self.limit
        data["used"] = self.used
        data["policyId"] = self.policyId
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ContextBundleBudget:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            limit=data["limit"],
            used=data["used"],
            policyId=data["policyId"],
        )


@dataclass(frozen=True)
class ContextBundleExcludedItem:
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    sourceId: str
    reason: ContextBundleExcludedItemReason
    tokenCount: int

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["sourceId"] = self.sourceId
        data["reason"] = self.reason.value
        data["tokenCount"] = self.tokenCount
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ContextBundleExcludedItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            sourceId=data["sourceId"],
            reason=ContextBundleExcludedItemReason(data["reason"]),
            tokenCount=data["tokenCount"],
        )


@dataclass(frozen=True)
class ContextBundleItem:
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    itemId: str
    sourceSystem: ContextBundleItemSourceSystem
    sourceId: str
    revision: str
    retrievedAt: str
    retrievedAs: str
    strategy: ContextBundleItemStrategy
    tokenCount: int
    inclusionReason: str
    trustClass: ContextBundleItemTrustClass
    contentRef: ArtifactRef
    relevanceScore: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["itemId"] = self.itemId
        data["sourceSystem"] = self.sourceSystem.value
        data["sourceId"] = self.sourceId
        data["revision"] = self.revision
        data["retrievedAt"] = self.retrievedAt
        data["retrievedAs"] = self.retrievedAs
        data["strategy"] = self.strategy.value
        data["tokenCount"] = self.tokenCount
        data["inclusionReason"] = self.inclusionReason
        data["trustClass"] = self.trustClass.value
        data["contentRef"] = self.contentRef.to_dict()
        if self.relevanceScore is not None:
            data["relevanceScore"] = self.relevanceScore
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ContextBundleItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            itemId=data["itemId"],
            sourceSystem=ContextBundleItemSourceSystem(data["sourceSystem"]),
            sourceId=data["sourceId"],
            revision=data["revision"],
            retrievedAt=data["retrievedAt"],
            retrievedAs=data["retrievedAs"],
            strategy=ContextBundleItemStrategy(data["strategy"]),
            tokenCount=data["tokenCount"],
            inclusionReason=data["inclusionReason"],
            trustClass=ContextBundleItemTrustClass(data["trustClass"]),
            contentRef=ArtifactRef.from_dict(data["contentRef"]),
            relevanceScore=None if data.get("relevanceScore") is None else data["relevanceScore"],
        )


@dataclass(frozen=True)
class ContextBundlePins:
    """
    Exact revisions the bundle was built from. Pinning is what makes a run reproducible:
    re-running against a moved branch or an edited page is a different experiment.

    Source: schemas/context/context-bundle.schema.json
    """

    repositories: list[ContextBundlePinsRepositoriesItem] | None = None
    issues: list[ContextBundlePinsIssuesItem] | None = None
    pages: list[ContextBundlePinsPagesItem] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.repositories is not None:
            data["repositories"] = [_i.to_dict() for _i in self.repositories]
        if self.issues is not None:
            data["issues"] = [_i.to_dict() for _i in self.issues]
        if self.pages is not None:
            data["pages"] = [_i.to_dict() for _i in self.pages]
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ContextBundlePins:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            repositories=None if data.get("repositories") is None else [ContextBundlePinsRepositoriesItem.from_dict(_i) for _i in data["repositories"]],
            issues=None if data.get("issues") is None else [ContextBundlePinsIssuesItem.from_dict(_i) for _i in data["issues"]],
            pages=None if data.get("pages") is None else [ContextBundlePinsPagesItem.from_dict(_i) for _i in data["pages"]],
        )


@dataclass(frozen=True)
class ContextBundlePinsIssuesItem:
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    key: str
    versionOrUpdatedAt: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["key"] = self.key
        data["versionOrUpdatedAt"] = self.versionOrUpdatedAt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ContextBundlePinsIssuesItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            key=data["key"],
            versionOrUpdatedAt=data["versionOrUpdatedAt"],
        )


@dataclass(frozen=True)
class ContextBundlePinsPagesItem:
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    pageId: str
    version: Any

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["pageId"] = self.pageId
        data["version"] = self.version
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ContextBundlePinsPagesItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            pageId=data["pageId"],
            version=data["version"],
        )


@dataclass(frozen=True)
class ContextBundlePinsRepositoriesItem:
    """
    Generated from schemas/context/context-bundle.schema.json.

    Source: schemas/context/context-bundle.schema.json
    """

    repositoryId: str
    ref: str
    commitSha: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["repositoryId"] = self.repositoryId
        data["ref"] = self.ref
        data["commitSha"] = self.commitSha
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ContextBundlePinsRepositoriesItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            repositoryId=data["repositoryId"],
            ref=data["ref"],
            commitSha=data["commitSha"],
        )


@dataclass(frozen=True)
class Correlation:
    """
    Telemetry correlation block. ADR-0003 §4: carried, never interpreted. ADR-0015 §7: these
    values are forbidden as metric labels. Carries the full four-part NodeRun key (ADR-0021 §6)
    so a log line, a span and a stored record name the same thing.

    Source: schemas/common/common.schema.json
    """

    runId: str
    nodeId: str
    iteration: int
    attempt: int
    agentId: str | None = None
    traceparent: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["runId"] = self.runId
        data["nodeId"] = self.nodeId
        data["iteration"] = self.iteration
        data["attempt"] = self.attempt
        if self.agentId is not None:
            data["agentId"] = self.agentId
        if self.traceparent is not None:
            data["traceparent"] = self.traceparent
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> Correlation:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            runId=data["runId"],
            nodeId=data["nodeId"],
            iteration=data["iteration"],
            attempt=data["attempt"],
            agentId=None if data.get("agentId") is None else data["agentId"],
            traceparent=None if data.get("traceparent") is None else data["traceparent"],
        )


@dataclass(frozen=True)
class ExecutionRequest:
    """
    Submitted to the execution interface (ADR-0003 §4). The shape encodes the six
    substitutability rules of ADR-0003 §3: argv arrays rather than shell strings, an explicit
    environment allowlist rather than inheritance, artifact references rather than paths, and
    mandatory timeout, limits and network policy.

    Source: schemas/execution/execution-request.schema.json
    """

    protocolVersion: str
    argv: list[str]
    cwd: str
    envAllowlist: dict[str, str]
    timeoutMs: int
    resourceLimits: ResourceLimits
    networkPolicy: NetworkPolicy
    correlation: Correlation
    stdinRef: ArtifactRef | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["argv"] = list(self.argv)
        data["cwd"] = self.cwd
        data["envAllowlist"] = dict(self.envAllowlist)
        data["timeoutMs"] = self.timeoutMs
        data["resourceLimits"] = self.resourceLimits.to_dict()
        data["networkPolicy"] = self.networkPolicy.to_dict()
        data["correlation"] = self.correlation.to_dict()
        if self.stdinRef is not None:
            data["stdinRef"] = self.stdinRef.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ExecutionRequest:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            argv=list(data["argv"]),
            cwd=data["cwd"],
            envAllowlist=dict(data["envAllowlist"]),
            timeoutMs=data["timeoutMs"],
            resourceLimits=ResourceLimits.from_dict(data["resourceLimits"]),
            networkPolicy=NetworkPolicy.from_dict(data["networkPolicy"]),
            correlation=Correlation.from_dict(data["correlation"]),
            stdinRef=None if data.get("stdinRef") is None else ArtifactRef.from_dict(data["stdinRef"]),
        )


@dataclass(frozen=True)
class ExecutionResult:
    """
    Returned by await() or poll() (ADR-0003 §4). A non-zero exit code is a result, not an error:
    a failing test is the normal case in a repair loop. The error taxonomy is reserved for the
    executor failing to run the command at all.

    Source: schemas/execution/execution-result.schema.json
    """

    protocolVersion: str
    status: ExecutionResultStatus
    stdoutRef: ArtifactRef
    stderrRef: ArtifactRef
    durationMs: int
    networkDenials: list[ExecutionResultNetworkDenialsItem]
    exitCode: int | None = None
    stdoutPreview: str | None = None
    stderrPreview: str | None = None
    resourceUsage: ResourceUsage | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["status"] = self.status.value
        data["stdoutRef"] = self.stdoutRef.to_dict()
        data["stderrRef"] = self.stderrRef.to_dict()
        data["durationMs"] = self.durationMs
        data["networkDenials"] = [_i.to_dict() for _i in self.networkDenials]
        if self.exitCode is not None:
            data["exitCode"] = self.exitCode
        if self.stdoutPreview is not None:
            data["stdoutPreview"] = self.stdoutPreview
        if self.stderrPreview is not None:
            data["stderrPreview"] = self.stderrPreview
        if self.resourceUsage is not None:
            data["resourceUsage"] = self.resourceUsage.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ExecutionResult:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            status=ExecutionResultStatus(data["status"]),
            stdoutRef=ArtifactRef.from_dict(data["stdoutRef"]),
            stderrRef=ArtifactRef.from_dict(data["stderrRef"]),
            durationMs=data["durationMs"],
            networkDenials=[ExecutionResultNetworkDenialsItem.from_dict(_i) for _i in data["networkDenials"]],
            exitCode=None if data.get("exitCode") is None else data["exitCode"],
            stdoutPreview=None if data.get("stdoutPreview") is None else data["stdoutPreview"],
            stderrPreview=None if data.get("stderrPreview") is None else data["stderrPreview"],
            resourceUsage=None if data.get("resourceUsage") is None else ResourceUsage.from_dict(data["resourceUsage"]),
        )


@dataclass(frozen=True)
class ExecutionResultNetworkDenialsItem:
    """
    Generated from schemas/execution/execution-result.schema.json.

    Source: schemas/execution/execution-result.schema.json
    """

    host: str
    count: int
    port: int | None = None
    firstSeenAt: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["host"] = self.host
        data["count"] = self.count
        if self.port is not None:
            data["port"] = self.port
        if self.firstSeenAt is not None:
            data["firstSeenAt"] = self.firstSeenAt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ExecutionResultNetworkDenialsItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            host=data["host"],
            count=data["count"],
            port=None if data.get("port") is None else data["port"],
            firstSeenAt=None if data.get("firstSeenAt") is None else data["firstSeenAt"],
        )


@dataclass(frozen=True)
class Graph:
    """
    A workflow, expressed as declarative data (ADR-0008 §1). Contains no code and no expressions
    beyond the restricted condition language. Definitions are immutable and content-addressed:
    editing produces a new version identified by the content hash of its canonical form, and a
    run pins the version it started with for its entire life. ADR-0008 §16: graphs are authored
    by humans. An agent may propose a definition as a reviewable artifact, but no agent may
    author, mutate or select a definition for a live run.

    Source: schemas/graph/graph.schema.json
    """

    schemaVersion: str
    graphId: str
    title: str
    entryNodeId: str
    nodes: list[GraphNode]
    edges: list[GraphEdge]
    description: str | None = None
    variables: dict[str, dict[str, Any]] | None = None
    defaultRetryPolicy: GraphRetryPolicy | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["graphId"] = self.graphId
        data["title"] = self.title
        data["entryNodeId"] = self.entryNodeId
        data["nodes"] = [_i.to_dict() for _i in self.nodes]
        data["edges"] = [_i.to_dict() for _i in self.edges]
        if self.description is not None:
            data["description"] = self.description
        if self.variables is not None:
            data["variables"] = dict(self.variables)
        if self.defaultRetryPolicy is not None:
            data["defaultRetryPolicy"] = self.defaultRetryPolicy.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> Graph:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            graphId=data["graphId"],
            title=data["title"],
            entryNodeId=data["entryNodeId"],
            nodes=[GraphNode.from_dict(_i) for _i in data["nodes"]],
            edges=[GraphEdge.from_dict(_i) for _i in data["edges"]],
            description=None if data.get("description") is None else data["description"],
            variables=None if data.get("variables") is None else dict(data["variables"]),
            defaultRetryPolicy=None if data.get("defaultRetryPolicy") is None else GraphRetryPolicy.from_dict(data["defaultRetryPolicy"]),
        )


@dataclass(frozen=True)
class GraphAgentNode:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    id_: str
    type_: str
    agentId: str
    objective: str
    outputSchema: dict[str, Any]
    requestedCapabilities: GraphAgentNodeRequestedCapabilities
    budgets: GraphAgentNodeBudgets
    title: str | None = None
    timeoutMs: int | None = None
    retryPolicy: GraphRetryPolicy | None = None
    modelPolicy: GraphAgentNodeModelPolicy | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["id"] = self.id_
        data["type"] = self.type_
        data["agentId"] = self.agentId
        data["objective"] = self.objective
        data["outputSchema"] = self.outputSchema
        data["requestedCapabilities"] = self.requestedCapabilities.to_dict()
        data["budgets"] = self.budgets.to_dict()
        if self.title is not None:
            data["title"] = self.title
        if self.timeoutMs is not None:
            data["timeoutMs"] = self.timeoutMs
        if self.retryPolicy is not None:
            data["retryPolicy"] = self.retryPolicy.to_dict()
        if self.modelPolicy is not None:
            data["modelPolicy"] = self.modelPolicy.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphAgentNode:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=data["id"],
            type_=data["type"],
            agentId=data["agentId"],
            objective=data["objective"],
            outputSchema=data["outputSchema"],
            requestedCapabilities=GraphAgentNodeRequestedCapabilities.from_dict(data["requestedCapabilities"]),
            budgets=GraphAgentNodeBudgets.from_dict(data["budgets"]),
            title=None if data.get("title") is None else data["title"],
            timeoutMs=None if data.get("timeoutMs") is None else data["timeoutMs"],
            retryPolicy=None if data.get("retryPolicy") is None else GraphRetryPolicy.from_dict(data["retryPolicy"]),
            modelPolicy=None if data.get("modelPolicy") is None else GraphAgentNodeModelPolicy.from_dict(data["modelPolicy"]),
        )


@dataclass(frozen=True)
class GraphAgentNodeBudgets:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    maxIterations: int
    maxTokens: int
    maxCostUnits: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["maxIterations"] = self.maxIterations
        data["maxTokens"] = self.maxTokens
        if self.maxCostUnits is not None:
            data["maxCostUnits"] = self.maxCostUnits
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphAgentNodeBudgets:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxIterations=data["maxIterations"],
            maxTokens=data["maxTokens"],
            maxCostUnits=None if data.get("maxCostUnits") is None else data["maxCostUnits"],
        )


@dataclass(frozen=True)
class GraphAgentNodeModelPolicy:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    selector: str | None = None
    modelRef: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.selector is not None:
            data["selector"] = self.selector
        if self.modelRef is not None:
            data["modelRef"] = self.modelRef
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphAgentNodeModelPolicy:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            selector=None if data.get("selector") is None else data["selector"],
            modelRef=None if data.get("modelRef") is None else data["modelRef"],
        )


@dataclass(frozen=True)
class GraphAgentNodeRequestedCapabilities:
    """
    What this node asks for. The effective grant is the intersection of this with the registry
    ceiling and platform policy (ADR-0014 §5); a node can only narrow, never widen.

    Source: schemas/graph/graph.schema.json
    """

    allowedTools: list[str]
    maxSideEffect: SideEffectClass
    commandProfiles: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["allowedTools"] = list(self.allowedTools)
        data["maxSideEffect"] = self.maxSideEffect.value
        if self.commandProfiles is not None:
            data["commandProfiles"] = list(self.commandProfiles)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphAgentNodeRequestedCapabilities:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            allowedTools=list(data["allowedTools"]),
            maxSideEffect=SideEffectClass(data["maxSideEffect"]),
            commandProfiles=None if data.get("commandProfiles") is None else list(data["commandProfiles"]),
        )


@dataclass(frozen=True)
class GraphApprovalNode:
    """
    A durable pause (ADR-0008 §13). The engine writes AWAITING_APPROVAL, releases the lease and
    stops; nothing is held in memory.

    Source: schemas/graph/graph.schema.json
    """

    id_: str
    type_: str
    requiredKind: GraphApprovalNodeRequiredKind
    subjectRef: str
    expiresAfterMs: int
    title: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["id"] = self.id_
        data["type"] = self.type_
        data["requiredKind"] = self.requiredKind.value
        data["subjectRef"] = self.subjectRef
        data["expiresAfterMs"] = self.expiresAfterMs
        if self.title is not None:
            data["title"] = self.title
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphApprovalNode:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=data["id"],
            type_=data["type"],
            requiredKind=GraphApprovalNodeRequiredKind(data["requiredKind"]),
            subjectRef=data["subjectRef"],
            expiresAfterMs=data["expiresAfterMs"],
            title=None if data.get("title") is None else data["title"],
        )


@dataclass(frozen=True)
class GraphContextNode:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    id_: str
    type_: str
    contextPolicyId: str
    tokenBudget: int
    title: str | None = None
    timeoutMs: int | None = None
    retryPolicy: GraphRetryPolicy | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["id"] = self.id_
        data["type"] = self.type_
        data["contextPolicyId"] = self.contextPolicyId
        data["tokenBudget"] = self.tokenBudget
        if self.title is not None:
            data["title"] = self.title
        if self.timeoutMs is not None:
            data["timeoutMs"] = self.timeoutMs
        if self.retryPolicy is not None:
            data["retryPolicy"] = self.retryPolicy.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphContextNode:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=data["id"],
            type_=data["type"],
            contextPolicyId=data["contextPolicyId"],
            tokenBudget=data["tokenBudget"],
            title=None if data.get("title") is None else data["title"],
            timeoutMs=None if data.get("timeoutMs") is None else data["timeoutMs"],
            retryPolicy=None if data.get("retryPolicy") is None else GraphRetryPolicy.from_dict(data["retryPolicy"]),
        )


@dataclass(frozen=True)
class GraphDecisionNode:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    id_: str
    type_: str
    title: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["id"] = self.id_
        data["type"] = self.type_
        if self.title is not None:
            data["title"] = self.title
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphDecisionNode:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=data["id"],
            type_=data["type"],
            title=None if data.get("title") is None else data["title"],
        )


@dataclass(frozen=True)
class GraphEdge:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    from_: str
    to: str
    condition: ConditionExpression | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["from"] = self.from_
        data["to"] = self.to
        if self.condition is not None:
            data["condition"] = self.condition.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphEdge:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            from_=data["from"],
            to=data["to"],
            condition=None if data.get("condition") is None else ConditionExpression.from_dict(data["condition"]),
        )


@dataclass(frozen=True)
class GraphLoopNode:
    """
    ADR-0008 §12: bounded, with a deterministic exit. A model may not decide that a loop is
    finished; a deterministic verification step decides. This is the repair loop the platform
    exists to run, and also the failure mode most likely to burn a day's budget in ten minutes.

    Source: schemas/graph/graph.schema.json
    """

    id_: str
    type_: str
    bodyNodeId: str
    maxIterations: int
    exitCondition: ConditionExpression
    title: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["id"] = self.id_
        data["type"] = self.type_
        data["bodyNodeId"] = self.bodyNodeId
        data["maxIterations"] = self.maxIterations
        data["exitCondition"] = self.exitCondition.to_dict()
        if self.title is not None:
            data["title"] = self.title
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphLoopNode:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=data["id"],
            type_=data["type"],
            bodyNodeId=data["bodyNodeId"],
            maxIterations=data["maxIterations"],
            exitCondition=ConditionExpression.from_dict(data["exitCondition"]),
            title=None if data.get("title") is None else data["title"],
        )


@dataclass(frozen=True)
class GraphNode:
    """
    ADR-0008 §3 defines a closed node taxonomy. Adding a node type requires an ADR, so this list
    is intentionally exhaustive. Union of mutually exclusive branches; exactly one branch's
    required fields are present in a valid document, which the JSON Schema enforces and this
    type does not.

    Source: schemas/graph/graph.schema.json
    """

    id_: str | None = None
    type_: GraphNodeType | None = None
    title: str | None = None
    timeoutMs: int | None = None
    retryPolicy: GraphRetryPolicy | None = None
    contextPolicyId: str | None = None
    tokenBudget: int | None = None
    agentId: str | None = None
    objective: str | None = None
    outputSchema: dict[str, Any] | None = None
    requestedCapabilities: GraphNodeRequestedCapabilities | None = None
    budgets: GraphNodeBudgets | None = None
    modelPolicy: GraphNodeModelPolicy | None = None
    toolId: str | None = None
    toolVersion: str | None = None
    input_: dict[str, Any] | None = None
    requiredKind: GraphNodeRequiredKind | None = None
    subjectRef: str | None = None
    expiresAfterMs: int | None = None
    bodyNodeId: str | None = None
    maxIterations: int | None = None
    exitCondition: ConditionExpression | None = None
    outcome: GraphNodeOutcome | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.id_ is not None:
            data["id"] = self.id_
        if self.type_ is not None:
            data["type"] = self.type_.value
        if self.title is not None:
            data["title"] = self.title
        if self.timeoutMs is not None:
            data["timeoutMs"] = self.timeoutMs
        if self.retryPolicy is not None:
            data["retryPolicy"] = self.retryPolicy.to_dict()
        if self.contextPolicyId is not None:
            data["contextPolicyId"] = self.contextPolicyId
        if self.tokenBudget is not None:
            data["tokenBudget"] = self.tokenBudget
        if self.agentId is not None:
            data["agentId"] = self.agentId
        if self.objective is not None:
            data["objective"] = self.objective
        if self.outputSchema is not None:
            data["outputSchema"] = self.outputSchema
        if self.requestedCapabilities is not None:
            data["requestedCapabilities"] = self.requestedCapabilities.to_dict()
        if self.budgets is not None:
            data["budgets"] = self.budgets.to_dict()
        if self.modelPolicy is not None:
            data["modelPolicy"] = self.modelPolicy.to_dict()
        if self.toolId is not None:
            data["toolId"] = self.toolId
        if self.toolVersion is not None:
            data["toolVersion"] = self.toolVersion
        if self.input_ is not None:
            data["input"] = self.input_
        if self.requiredKind is not None:
            data["requiredKind"] = self.requiredKind.value
        if self.subjectRef is not None:
            data["subjectRef"] = self.subjectRef
        if self.expiresAfterMs is not None:
            data["expiresAfterMs"] = self.expiresAfterMs
        if self.bodyNodeId is not None:
            data["bodyNodeId"] = self.bodyNodeId
        if self.maxIterations is not None:
            data["maxIterations"] = self.maxIterations
        if self.exitCondition is not None:
            data["exitCondition"] = self.exitCondition.to_dict()
        if self.outcome is not None:
            data["outcome"] = self.outcome.value
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphNode:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=None if data.get("id") is None else data["id"],
            type_=None if data.get("type") is None else GraphNodeType(data["type"]),
            title=None if data.get("title") is None else data["title"],
            timeoutMs=None if data.get("timeoutMs") is None else data["timeoutMs"],
            retryPolicy=None if data.get("retryPolicy") is None else GraphRetryPolicy.from_dict(data["retryPolicy"]),
            contextPolicyId=None if data.get("contextPolicyId") is None else data["contextPolicyId"],
            tokenBudget=None if data.get("tokenBudget") is None else data["tokenBudget"],
            agentId=None if data.get("agentId") is None else data["agentId"],
            objective=None if data.get("objective") is None else data["objective"],
            outputSchema=None if data.get("outputSchema") is None else data["outputSchema"],
            requestedCapabilities=None if data.get("requestedCapabilities") is None else GraphNodeRequestedCapabilities.from_dict(data["requestedCapabilities"]),
            budgets=None if data.get("budgets") is None else GraphNodeBudgets.from_dict(data["budgets"]),
            modelPolicy=None if data.get("modelPolicy") is None else GraphNodeModelPolicy.from_dict(data["modelPolicy"]),
            toolId=None if data.get("toolId") is None else data["toolId"],
            toolVersion=None if data.get("toolVersion") is None else data["toolVersion"],
            input_=None if data.get("input") is None else data["input"],
            requiredKind=None if data.get("requiredKind") is None else GraphNodeRequiredKind(data["requiredKind"]),
            subjectRef=None if data.get("subjectRef") is None else data["subjectRef"],
            expiresAfterMs=None if data.get("expiresAfterMs") is None else data["expiresAfterMs"],
            bodyNodeId=None if data.get("bodyNodeId") is None else data["bodyNodeId"],
            maxIterations=None if data.get("maxIterations") is None else data["maxIterations"],
            exitCondition=None if data.get("exitCondition") is None else ConditionExpression.from_dict(data["exitCondition"]),
            outcome=None if data.get("outcome") is None else GraphNodeOutcome(data["outcome"]),
        )


@dataclass(frozen=True)
class GraphNodeBudgets:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    maxIterations: int
    maxTokens: int
    maxCostUnits: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["maxIterations"] = self.maxIterations
        data["maxTokens"] = self.maxTokens
        if self.maxCostUnits is not None:
            data["maxCostUnits"] = self.maxCostUnits
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphNodeBudgets:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxIterations=data["maxIterations"],
            maxTokens=data["maxTokens"],
            maxCostUnits=None if data.get("maxCostUnits") is None else data["maxCostUnits"],
        )


@dataclass(frozen=True)
class GraphNodeModelPolicy:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    selector: str | None = None
    modelRef: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.selector is not None:
            data["selector"] = self.selector
        if self.modelRef is not None:
            data["modelRef"] = self.modelRef
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphNodeModelPolicy:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            selector=None if data.get("selector") is None else data["selector"],
            modelRef=None if data.get("modelRef") is None else data["modelRef"],
        )


@dataclass(frozen=True)
class GraphNodeRequestedCapabilities:
    """
    What this node asks for. The effective grant is the intersection of this with the registry
    ceiling and platform policy (ADR-0014 §5); a node can only narrow, never widen.

    Source: schemas/graph/graph.schema.json
    """

    allowedTools: list[str]
    maxSideEffect: SideEffectClass
    commandProfiles: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["allowedTools"] = list(self.allowedTools)
        data["maxSideEffect"] = self.maxSideEffect.value
        if self.commandProfiles is not None:
            data["commandProfiles"] = list(self.commandProfiles)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphNodeRequestedCapabilities:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            allowedTools=list(data["allowedTools"]),
            maxSideEffect=SideEffectClass(data["maxSideEffect"]),
            commandProfiles=None if data.get("commandProfiles") is None else list(data["commandProfiles"]),
        )


@dataclass(frozen=True)
class GraphRetryPolicy:
    """
    ADR-0008 §11. Policy lives in the definition and is therefore versioned and pinned; retry
    state lives on the node. Retry is driven by category, never by message text.

    Source: schemas/graph/graph.schema.json
    """

    maxAttempts: int
    backoff: GraphRetryPolicyBackoff
    retryableCategories: list[GraphRetryPolicyRetryableCategoriesItem]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["maxAttempts"] = self.maxAttempts
        data["backoff"] = self.backoff.to_dict()
        data["retryableCategories"] = [_i.value for _i in self.retryableCategories]
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphRetryPolicy:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxAttempts=data["maxAttempts"],
            backoff=GraphRetryPolicyBackoff.from_dict(data["backoff"]),
            retryableCategories=[GraphRetryPolicyRetryableCategoriesItem(_i) for _i in data["retryableCategories"]],
        )


@dataclass(frozen=True)
class GraphRetryPolicyBackoff:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    strategy: GraphRetryPolicyBackoffStrategy
    initialDelayMs: int
    maxDelayMs: int | None = None
    jitter: bool | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["strategy"] = self.strategy.value
        data["initialDelayMs"] = self.initialDelayMs
        if self.maxDelayMs is not None:
            data["maxDelayMs"] = self.maxDelayMs
        if self.jitter is not None:
            data["jitter"] = self.jitter
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphRetryPolicyBackoff:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            strategy=GraphRetryPolicyBackoffStrategy(data["strategy"]),
            initialDelayMs=data["initialDelayMs"],
            maxDelayMs=None if data.get("maxDelayMs") is None else data["maxDelayMs"],
            jitter=None if data.get("jitter") is None else data["jitter"],
        )


@dataclass(frozen=True)
class GraphRun:
    """
    The durable record of a graph run (ADR-0008). This is the authoritative state: the HTTP call
    that dispatches a node attempt is an optimisation, not the record (ADR-0004 §7). Only the
    holder of the lease may transition a run.

    Source: schemas/graph/graph-run.schema.json
    """

    schemaVersion: str
    runId: str
    version: int
    graphId: str
    graphVersionHash: str
    status: GraphRunStatus
    epoch: int
    repositories: list[str]
    cancellationRequested: bool
    initiatedBy: str
    createdAt: str
    updatedAt: str
    agentPins: dict[str, str] | None = None
    lease: GraphRunLease | None = None
    workItemRef: GraphRunWorkItemRef | None = None
    workspaceId: str | None = None
    variables: dict[str, Any] | None = None
    currentNodeId: str | None = None
    latestCheckpointId: str | None = None
    completedAt: str | None = None
    failure: GraphRunFailure | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["runId"] = self.runId
        data["version"] = self.version
        data["graphId"] = self.graphId
        data["graphVersionHash"] = self.graphVersionHash
        data["status"] = self.status.value
        data["epoch"] = self.epoch
        data["repositories"] = list(self.repositories)
        data["cancellationRequested"] = self.cancellationRequested
        data["initiatedBy"] = self.initiatedBy
        data["createdAt"] = self.createdAt
        data["updatedAt"] = self.updatedAt
        if self.agentPins is not None:
            data["agentPins"] = dict(self.agentPins)
        if self.lease is not None:
            data["lease"] = self.lease.to_dict()
        if self.workItemRef is not None:
            data["workItemRef"] = self.workItemRef.to_dict()
        if self.workspaceId is not None:
            data["workspaceId"] = self.workspaceId
        if self.variables is not None:
            data["variables"] = self.variables
        if self.currentNodeId is not None:
            data["currentNodeId"] = self.currentNodeId
        if self.latestCheckpointId is not None:
            data["latestCheckpointId"] = self.latestCheckpointId
        if self.completedAt is not None:
            data["completedAt"] = self.completedAt
        if self.failure is not None:
            data["failure"] = self.failure.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphRun:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            runId=data["runId"],
            version=data["version"],
            graphId=data["graphId"],
            graphVersionHash=data["graphVersionHash"],
            status=GraphRunStatus(data["status"]),
            epoch=data["epoch"],
            repositories=list(data["repositories"]),
            cancellationRequested=data["cancellationRequested"],
            initiatedBy=data["initiatedBy"],
            createdAt=data["createdAt"],
            updatedAt=data["updatedAt"],
            agentPins=None if data.get("agentPins") is None else dict(data["agentPins"]),
            lease=None if data.get("lease") is None else GraphRunLease.from_dict(data["lease"]),
            workItemRef=None if data.get("workItemRef") is None else GraphRunWorkItemRef.from_dict(data["workItemRef"]),
            workspaceId=None if data.get("workspaceId") is None else data["workspaceId"],
            variables=None if data.get("variables") is None else data["variables"],
            currentNodeId=None if data.get("currentNodeId") is None else data["currentNodeId"],
            latestCheckpointId=None if data.get("latestCheckpointId") is None else data["latestCheckpointId"],
            completedAt=None if data.get("completedAt") is None else data["completedAt"],
            failure=None if data.get("failure") is None else GraphRunFailure.from_dict(data["failure"]),
        )


@dataclass(frozen=True)
class GraphRunFailure:
    """
    Generated from schemas/graph/graph-run.schema.json.

    Source: schemas/graph/graph-run.schema.json
    """

    category: FailureCategory
    message: str
    nodeId: str | None = None
    detailsRef: ArtifactRef | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["category"] = self.category.value
        data["message"] = self.message
        if self.nodeId is not None:
            data["nodeId"] = self.nodeId
        if self.detailsRef is not None:
            data["detailsRef"] = self.detailsRef.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphRunFailure:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            category=FailureCategory(data["category"]),
            message=data["message"],
            nodeId=None if data.get("nodeId") is None else data["nodeId"],
            detailsRef=None if data.get("detailsRef") is None else ArtifactRef.from_dict(data["detailsRef"]),
        )


@dataclass(frozen=True)
class GraphRunLease:
    """
    ADR-0008 §8: exactly one orchestrator per run. This is a security control as well as a
    correctness one, because two orchestrators would produce duplicate external side effects.

    Source: schemas/graph/graph-run.schema.json
    """

    holderId: str
    acquiredAt: str
    expiresAt: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["holderId"] = self.holderId
        data["acquiredAt"] = self.acquiredAt
        data["expiresAt"] = self.expiresAt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphRunLease:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            holderId=data["holderId"],
            acquiredAt=data["acquiredAt"],
            expiresAt=data["expiresAt"],
        )


@dataclass(frozen=True)
class GraphRunWorkItemRef:
    """
    The Jira issue this run serves, where there is one. Jira remains the system of record; this
    is a reference, not a copy.

    Source: schemas/graph/graph-run.schema.json
    """

    system: str
    key: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["system"] = self.system
        data["key"] = self.key
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphRunWorkItemRef:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            system=data["system"],
            key=data["key"],
        )


@dataclass(frozen=True)
class GraphTerminalNode:
    """
    Generated from schemas/graph/graph.schema.json.

    Source: schemas/graph/graph.schema.json
    """

    id_: str
    type_: str
    outcome: GraphTerminalNodeOutcome
    title: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["id"] = self.id_
        data["type"] = self.type_
        data["outcome"] = self.outcome.value
        if self.title is not None:
            data["title"] = self.title
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphTerminalNode:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=data["id"],
            type_=data["type"],
            outcome=GraphTerminalNodeOutcome(data["outcome"]),
            title=None if data.get("title") is None else data["title"],
        )


@dataclass(frozen=True)
class GraphToolNode:
    """
    A deterministic side effect. Exactly one tool, fixed in the definition. This is the escape
    hatch for behaviour the condition language cannot express (ADR-0008 Consequences).

    Source: schemas/graph/graph.schema.json
    """

    id_: str
    type_: str
    toolId: str
    toolVersion: str
    input_: dict[str, Any]
    title: str | None = None
    timeoutMs: int | None = None
    retryPolicy: GraphRetryPolicy | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["id"] = self.id_
        data["type"] = self.type_
        data["toolId"] = self.toolId
        data["toolVersion"] = self.toolVersion
        data["input"] = self.input_
        if self.title is not None:
            data["title"] = self.title
        if self.timeoutMs is not None:
            data["timeoutMs"] = self.timeoutMs
        if self.retryPolicy is not None:
            data["retryPolicy"] = self.retryPolicy.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> GraphToolNode:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            id_=data["id"],
            type_=data["type"],
            toolId=data["toolId"],
            toolVersion=data["toolVersion"],
            input_=data["input"],
            title=None if data.get("title") is None else data["title"],
            timeoutMs=None if data.get("timeoutMs") is None else data["timeoutMs"],
            retryPolicy=None if data.get("retryPolicy") is None else GraphRetryPolicy.from_dict(data["retryPolicy"]),
        )


@dataclass(frozen=True)
class ModelRequest:
    """
    The provider-neutral request accepted by the model gateway (ADR-0010 §1). Provider-specific
    SDK calls live behind the gateway and nowhere else. ADR-0010 §2: the gateway does not run
    the tool-calling loop — it normalises tool calls into requests, and the tool layer decides
    and executes. Most agent frameworks do run the loop in this position, so this is the default
    to resist.

    Source: schemas/model/model-request.schema.json
    """

    protocolVersion: str
    modelRef: str
    system: list[str]
    messages: list[ModelRequestMessagesItem]
    budgets: ModelRequestBudgets
    correlation: Correlation
    tools: list[ModelRequestToolsItem] | None = None
    toolChoice: Any | None = None
    outputContract: dict[str, Any] | None = None
    params: ModelRequestParams | None = None
    providerOptions: ModelRequestProviderOptions | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["modelRef"] = self.modelRef
        data["system"] = list(self.system)
        data["messages"] = [_i.to_dict() for _i in self.messages]
        data["budgets"] = self.budgets.to_dict()
        data["correlation"] = self.correlation.to_dict()
        if self.tools is not None:
            data["tools"] = [_i.to_dict() for _i in self.tools]
        if self.toolChoice is not None:
            data["toolChoice"] = self.toolChoice
        if self.outputContract is not None:
            data["outputContract"] = self.outputContract
        if self.params is not None:
            data["params"] = self.params.to_dict()
        if self.providerOptions is not None:
            data["providerOptions"] = self.providerOptions.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelRequest:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            modelRef=data["modelRef"],
            system=list(data["system"]),
            messages=[ModelRequestMessagesItem.from_dict(_i) for _i in data["messages"]],
            budgets=ModelRequestBudgets.from_dict(data["budgets"]),
            correlation=Correlation.from_dict(data["correlation"]),
            tools=None if data.get("tools") is None else [ModelRequestToolsItem.from_dict(_i) for _i in data["tools"]],
            toolChoice=None if data.get("toolChoice") is None else data["toolChoice"],
            outputContract=None if data.get("outputContract") is None else data["outputContract"],
            params=None if data.get("params") is None else ModelRequestParams.from_dict(data["params"]),
            providerOptions=None if data.get("providerOptions") is None else ModelRequestProviderOptions.from_dict(data["providerOptions"]),
        )


@dataclass(frozen=True)
class ModelRequestBudgets:
    """
    Generated from schemas/model/model-request.schema.json.

    Source: schemas/model/model-request.schema.json
    """

    maxInputTokens: int
    maxOutputTokens: int
    timeoutMs: int

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["maxInputTokens"] = self.maxInputTokens
        data["maxOutputTokens"] = self.maxOutputTokens
        data["timeoutMs"] = self.timeoutMs
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelRequestBudgets:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxInputTokens=data["maxInputTokens"],
            maxOutputTokens=data["maxOutputTokens"],
            timeoutMs=data["timeoutMs"],
        )


@dataclass(frozen=True)
class ModelRequestContentBlock:
    """
    Union of mutually exclusive branches; exactly one branch's required fields are present in a
    valid document, which the JSON Schema enforces and this type does not.

    Source: schemas/model/model-request.schema.json
    """

    type_: ModelRequestContentBlockType | None = None
    text: str | None = None
    ref: ArtifactRef | None = None
    toolCallId: str | None = None
    name: str | None = None
    arguments: dict[str, Any] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.type_ is not None:
            data["type"] = self.type_.value
        if self.text is not None:
            data["text"] = self.text
        if self.ref is not None:
            data["ref"] = self.ref.to_dict()
        if self.toolCallId is not None:
            data["toolCallId"] = self.toolCallId
        if self.name is not None:
            data["name"] = self.name
        if self.arguments is not None:
            data["arguments"] = self.arguments
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelRequestContentBlock:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            type_=None if data.get("type") is None else ModelRequestContentBlockType(data["type"]),
            text=None if data.get("text") is None else data["text"],
            ref=None if data.get("ref") is None else ArtifactRef.from_dict(data["ref"]),
            toolCallId=None if data.get("toolCallId") is None else data["toolCallId"],
            name=None if data.get("name") is None else data["name"],
            arguments=None if data.get("arguments") is None else data["arguments"],
        )


@dataclass(frozen=True)
class ModelRequestMessagesItem:
    """
    Generated from schemas/model/model-request.schema.json.

    Source: schemas/model/model-request.schema.json
    """

    role: ModelRequestMessagesItemRole
    content: list[ModelRequestContentBlock]
    toolCallId: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["role"] = self.role.value
        data["content"] = [_i.to_dict() for _i in self.content]
        if self.toolCallId is not None:
            data["toolCallId"] = self.toolCallId
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelRequestMessagesItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            role=ModelRequestMessagesItemRole(data["role"]),
            content=[ModelRequestContentBlock.from_dict(_i) for _i in data["content"]],
            toolCallId=None if data.get("toolCallId") is None else data["toolCallId"],
        )


@dataclass(frozen=True)
class ModelRequestParams:
    """
    Generated from schemas/model/model-request.schema.json.

    Source: schemas/model/model-request.schema.json
    """

    temperature: float | None = None
    topP: float | None = None
    maxOutputTokens: int | None = None
    stopSequences: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.temperature is not None:
            data["temperature"] = self.temperature
        if self.topP is not None:
            data["topP"] = self.topP
        if self.maxOutputTokens is not None:
            data["maxOutputTokens"] = self.maxOutputTokens
        if self.stopSequences is not None:
            data["stopSequences"] = list(self.stopSequences)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelRequestParams:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            temperature=None if data.get("temperature") is None else data["temperature"],
            topP=None if data.get("topP") is None else data["topP"],
            maxOutputTokens=None if data.get("maxOutputTokens") is None else data["maxOutputTokens"],
            stopSequences=None if data.get("stopSequences") is None else list(data["stopSequences"]),
        )


@dataclass(frozen=True)
class ModelRequestProviderOptions:
    """
    A narrow, explicitly-labelled escape hatch (ADR-0010 §4). Anything placed here is by
    definition not portable across providers, and the name says so.

    Source: schemas/model/model-request.schema.json
    """

    provider: str
    options: dict[str, Any]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["provider"] = self.provider
        data["options"] = self.options
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelRequestProviderOptions:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            provider=data["provider"],
            options=data["options"],
        )


@dataclass(frozen=True)
class ModelRequestToolsItem:
    """
    Generated from schemas/model/model-request.schema.json.

    Source: schemas/model/model-request.schema.json
    """

    name: str
    description: str
    inputSchema: dict[str, Any]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["name"] = self.name
        data["description"] = self.description
        data["inputSchema"] = self.inputSchema
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelRequestToolsItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            name=data["name"],
            description=data["description"],
            inputSchema=data["inputSchema"],
        )


@dataclass(frozen=True)
class ModelResponse:
    """
    The provider-neutral response produced by the model gateway (ADR-0010 §1). Usage and cost
    recorded here are the gateway's own measurements and are authoritative: ADR-0004 §9 forbids
    taking them from the agent runtime's summary.

    Source: schemas/model/model-response.schema.json
    """

    protocolVersion: str
    modelInvocationId: str
    finishReason: ModelResponseFinishReason
    usage: ModelResponseUsage
    providerModelId: str
    latencyMs: int
    content: list[ModelResponseContentItem] | None = None
    toolCalls: list[ModelResponseToolCallsItem] | None = None
    cost: ModelResponseCost | None = None
    error: ModelResponseError | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["modelInvocationId"] = self.modelInvocationId
        data["finishReason"] = self.finishReason.value
        data["usage"] = self.usage.to_dict()
        data["providerModelId"] = self.providerModelId
        data["latencyMs"] = self.latencyMs
        if self.content is not None:
            data["content"] = [_i.to_dict() for _i in self.content]
        if self.toolCalls is not None:
            data["toolCalls"] = [_i.to_dict() for _i in self.toolCalls]
        if self.cost is not None:
            data["cost"] = self.cost.to_dict()
        if self.error is not None:
            data["error"] = self.error.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelResponse:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            modelInvocationId=data["modelInvocationId"],
            finishReason=ModelResponseFinishReason(data["finishReason"]),
            usage=ModelResponseUsage.from_dict(data["usage"]),
            providerModelId=data["providerModelId"],
            latencyMs=data["latencyMs"],
            content=None if data.get("content") is None else [ModelResponseContentItem.from_dict(_i) for _i in data["content"]],
            toolCalls=None if data.get("toolCalls") is None else [ModelResponseToolCallsItem.from_dict(_i) for _i in data["toolCalls"]],
            cost=None if data.get("cost") is None else ModelResponseCost.from_dict(data["cost"]),
            error=None if data.get("error") is None else ModelResponseError.from_dict(data["error"]),
        )


@dataclass(frozen=True)
class ModelResponseContentItem:
    """
    Union of mutually exclusive branches; exactly one branch's required fields are present in a
    valid document, which the JSON Schema enforces and this type does not.

    Source: schemas/model/model-response.schema.json
    """

    type_: ModelResponseContentItemType | None = None
    text: str | None = None
    toolCallId: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.type_ is not None:
            data["type"] = self.type_.value
        if self.text is not None:
            data["text"] = self.text
        if self.toolCallId is not None:
            data["toolCallId"] = self.toolCallId
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelResponseContentItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            type_=None if data.get("type") is None else ModelResponseContentItemType(data["type"]),
            text=None if data.get("text") is None else data["text"],
            toolCallId=None if data.get("toolCallId") is None else data["toolCallId"],
        )


@dataclass(frozen=True)
class ModelResponseCost:
    """
    Computed by the gateway from recorded usage (ADR-0011 §5), never reported by the caller.

    Source: schemas/model/model-response.schema.json
    """

    amount: float
    currency: str
    estimated: bool

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["amount"] = self.amount
        data["currency"] = self.currency
        data["estimated"] = self.estimated
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelResponseCost:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            amount=data["amount"],
            currency=data["currency"],
            estimated=data["estimated"],
        )


@dataclass(frozen=True)
class ModelResponseError:
    """
    Generated from schemas/model/model-response.schema.json.

    Source: schemas/model/model-response.schema.json
    """

    category: ModelResponseErrorCategory
    message: str
    retryAfterMs: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["category"] = self.category.value
        data["message"] = self.message
        if self.retryAfterMs is not None:
            data["retryAfterMs"] = self.retryAfterMs
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelResponseError:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            category=ModelResponseErrorCategory(data["category"]),
            message=data["message"],
            retryAfterMs=None if data.get("retryAfterMs") is None else data["retryAfterMs"],
        )


@dataclass(frozen=True)
class ModelResponseToolCallsItem:
    """
    Generated from schemas/model/model-response.schema.json.

    Source: schemas/model/model-response.schema.json
    """

    toolCallId: str
    name: str
    arguments: dict[str, Any]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["toolCallId"] = self.toolCallId
        data["name"] = self.name
        data["arguments"] = self.arguments
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelResponseToolCallsItem:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            toolCallId=data["toolCallId"],
            name=data["name"],
            arguments=data["arguments"],
        )


@dataclass(frozen=True)
class ModelResponseUsage:
    """
    Generated from schemas/model/model-response.schema.json.

    Source: schemas/model/model-response.schema.json
    """

    inputTokens: int
    outputTokens: int
    cachedInputTokens: int | None = None
    reasoningTokens: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["inputTokens"] = self.inputTokens
        data["outputTokens"] = self.outputTokens
        if self.cachedInputTokens is not None:
            data["cachedInputTokens"] = self.cachedInputTokens
        if self.reasoningTokens is not None:
            data["reasoningTokens"] = self.reasoningTokens
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ModelResponseUsage:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            inputTokens=data["inputTokens"],
            outputTokens=data["outputTokens"],
            cachedInputTokens=None if data.get("cachedInputTokens") is None else data["cachedInputTokens"],
            reasoningTokens=None if data.get("reasoningTokens") is None else data["reasoningTokens"],
        )


@dataclass(frozen=True)
class NetworkPolicy:
    """
    ADR-0003 §3: mandatory on every execution request, so connectivity is a decision rather than
    a default. Union of mutually exclusive branches; exactly one branch's required fields are
    present in a valid document, which the JSON Schema enforces and this type does not.

    Source: schemas/common/common.schema.json
    """

    mode: NetworkPolicyMode | None = None
    hosts: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.mode is not None:
            data["mode"] = self.mode.value
        if self.hosts is not None:
            data["hosts"] = list(self.hosts)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> NetworkPolicy:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            mode=None if data.get("mode") is None else NetworkPolicyMode(data["mode"]),
            hosts=None if data.get("hosts") is None else list(data["hosts"]),
        )


@dataclass(frozen=True)
class NodeRun:
    """
    The durable record of one node attempt (ADR-0008 §7). Identity is the four-part key (runId,
    nodeId, iteration, attempt) fixed by ADR-0021 §1. Records are never overwritten by a retry:
    each attempt keeps its own record, because evaluation depends on being able to see how
    failures evolved.

    Source: schemas/graph/node-run.schema.json
    """

    schemaVersion: str
    runId: str
    nodeId: str
    iteration: int
    attempt: int
    version: int
    nodeType: NodeRunNodeType
    status: NodeRunStatus
    capabilityGrantId: str | None = None
    contextBundleRef: str | None = None
    claimedOutput: dict[str, Any] | None = None
    validatedOutput: dict[str, Any] | None = None
    outputValid: bool | None = None
    stopReason: NodeRunStopReason | None = None
    failure: GraphRunFailure | None = None
    nextAttemptAt: str | None = None
    lastFailureCategory: FailureCategory | None = None
    indeterminateReason: NodeRunIndeterminateReason | None = None
    deadline: str | None = None
    startedAt: str | None = None
    endedAt: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["runId"] = self.runId
        data["nodeId"] = self.nodeId
        data["iteration"] = self.iteration
        data["attempt"] = self.attempt
        data["version"] = self.version
        data["nodeType"] = self.nodeType.value
        data["status"] = self.status.value
        if self.capabilityGrantId is not None:
            data["capabilityGrantId"] = self.capabilityGrantId
        if self.contextBundleRef is not None:
            data["contextBundleRef"] = self.contextBundleRef
        if self.claimedOutput is not None:
            data["claimedOutput"] = self.claimedOutput
        if self.validatedOutput is not None:
            data["validatedOutput"] = self.validatedOutput
        if self.outputValid is not None:
            data["outputValid"] = self.outputValid
        if self.stopReason is not None:
            data["stopReason"] = self.stopReason.value
        if self.failure is not None:
            data["failure"] = self.failure.to_dict()
        if self.nextAttemptAt is not None:
            data["nextAttemptAt"] = self.nextAttemptAt
        if self.lastFailureCategory is not None:
            data["lastFailureCategory"] = self.lastFailureCategory.value
        if self.indeterminateReason is not None:
            data["indeterminateReason"] = self.indeterminateReason.to_dict()
        if self.deadline is not None:
            data["deadline"] = self.deadline
        if self.startedAt is not None:
            data["startedAt"] = self.startedAt
        if self.endedAt is not None:
            data["endedAt"] = self.endedAt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> NodeRun:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            runId=data["runId"],
            nodeId=data["nodeId"],
            iteration=data["iteration"],
            attempt=data["attempt"],
            version=data["version"],
            nodeType=NodeRunNodeType(data["nodeType"]),
            status=NodeRunStatus(data["status"]),
            capabilityGrantId=None if data.get("capabilityGrantId") is None else data["capabilityGrantId"],
            contextBundleRef=None if data.get("contextBundleRef") is None else data["contextBundleRef"],
            claimedOutput=None if data.get("claimedOutput") is None else data["claimedOutput"],
            validatedOutput=None if data.get("validatedOutput") is None else data["validatedOutput"],
            outputValid=None if data.get("outputValid") is None else data["outputValid"],
            stopReason=None if data.get("stopReason") is None else NodeRunStopReason(data["stopReason"]),
            failure=None if data.get("failure") is None else GraphRunFailure.from_dict(data["failure"]),
            nextAttemptAt=None if data.get("nextAttemptAt") is None else data["nextAttemptAt"],
            lastFailureCategory=None if data.get("lastFailureCategory") is None else FailureCategory(data["lastFailureCategory"]),
            indeterminateReason=None if data.get("indeterminateReason") is None else NodeRunIndeterminateReason.from_dict(data["indeterminateReason"]),
            deadline=None if data.get("deadline") is None else data["deadline"],
            startedAt=None if data.get("startedAt") is None else data["startedAt"],
            endedAt=None if data.get("endedAt") is None else data["endedAt"],
        )


@dataclass(frozen=True)
class NodeRunIndeterminateReason:
    """
    Populated only when status is INDETERMINATE. ADR-0008 Security Impact: this must be visible
    to an operator. A silently swallowed indeterminate side effect is worse than a loud failure.

    Source: schemas/graph/node-run.schema.json
    """

    toolId: str
    idempotencyKey: str
    intentRecordedAt: str
    reconciliationHint: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["toolId"] = self.toolId
        data["idempotencyKey"] = self.idempotencyKey
        data["intentRecordedAt"] = self.intentRecordedAt
        if self.reconciliationHint is not None:
            data["reconciliationHint"] = self.reconciliationHint
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> NodeRunIndeterminateReason:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            toolId=data["toolId"],
            idempotencyKey=data["idempotencyKey"],
            intentRecordedAt=data["intentRecordedAt"],
            reconciliationHint=None if data.get("reconciliationHint") is None else data["reconciliationHint"],
        )


@dataclass(frozen=True)
class PathScopeEntry:
    """
    Allow/deny globs for one repository. Globs are repository-relative: 'src/**' inside this
    entry can only ever mean that path inside the repository the entry is keyed by. Glob
    semantics are restricted (ADR-0023 §4): '*' matches within one segment, '**' matches whole
    segments, and there is no brace expansion or regular-expression syntax.

    Source: schemas/common/common.schema.json
    """

    allow: list[str] | None = None
    deny: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.allow is not None:
            data["allow"] = list(self.allow)
        if self.deny is not None:
            data["deny"] = list(self.deny)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> PathScopeEntry:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            allow=None if data.get("allow") is None else list(data["allow"]),
            deny=None if data.get("deny") is None else list(data["deny"]),
        )


@dataclass(frozen=True)
class RepoRef:
    """
    Identifies what to materialise into a workspace. With baseSha and the patch series this
    reconstructs a workspace (ADR-0003 §6).

    Source: schemas/common/common.schema.json
    """

    repositoryId: str
    remote: str
    ref: str
    commitSha: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["repositoryId"] = self.repositoryId
        data["remote"] = self.remote
        data["ref"] = self.ref
        if self.commitSha is not None:
            data["commitSha"] = self.commitSha
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> RepoRef:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            repositoryId=data["repositoryId"],
            remote=data["remote"],
            ref=data["ref"],
            commitSha=None if data.get("commitSha") is None else data["commitSha"],
        )


@dataclass(frozen=True)
class RepositoryRecord:
    """
    A repository the platform is allowed to work in (ADR-0014). The record is the capability
    ceiling for that repository. ADR-0014 §5 fixes the precedence: platform policy, then
    trust-level ceiling, then this record, then the graph node definition — each layer may only
    narrow. Files resident in the target repository affect context only and have zero capability
    effect, which is what stops a repository the platform is editing from rewriting the
    configuration that governs editing it.

    Source: schemas/registry/repository-record.schema.json
    """

    schemaVersion: str
    repositoryId: str
    displayName: str
    remotes: list[str]
    defaultBranch: str
    trustLevel: TrustLevel
    toolImage: str
    commandProfiles: list[CommandProfile]
    capabilityCeiling: RepositoryRecordCapabilityCeiling
    contextPolicy: RepositoryRecordContextPolicy
    verification: RepositoryRecordVerification
    owners: list[str]
    status: RepositoryRecordStatus
    registeredBy: str
    registeredAt: str
    configHash: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["repositoryId"] = self.repositoryId
        data["displayName"] = self.displayName
        data["remotes"] = list(self.remotes)
        data["defaultBranch"] = self.defaultBranch
        data["trustLevel"] = self.trustLevel.value
        data["toolImage"] = self.toolImage
        data["commandProfiles"] = [_i.to_dict() for _i in self.commandProfiles]
        data["capabilityCeiling"] = self.capabilityCeiling.to_dict()
        data["contextPolicy"] = self.contextPolicy.to_dict()
        data["verification"] = self.verification.to_dict()
        data["owners"] = list(self.owners)
        data["status"] = self.status.value
        data["registeredBy"] = self.registeredBy
        data["registeredAt"] = self.registeredAt
        data["configHash"] = self.configHash
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> RepositoryRecord:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            repositoryId=data["repositoryId"],
            displayName=data["displayName"],
            remotes=list(data["remotes"]),
            defaultBranch=data["defaultBranch"],
            trustLevel=TrustLevel(data["trustLevel"]),
            toolImage=data["toolImage"],
            commandProfiles=[CommandProfile.from_dict(_i) for _i in data["commandProfiles"]],
            capabilityCeiling=RepositoryRecordCapabilityCeiling.from_dict(data["capabilityCeiling"]),
            contextPolicy=RepositoryRecordContextPolicy.from_dict(data["contextPolicy"]),
            verification=RepositoryRecordVerification.from_dict(data["verification"]),
            owners=list(data["owners"]),
            status=RepositoryRecordStatus(data["status"]),
            registeredBy=data["registeredBy"],
            registeredAt=data["registeredAt"],
            configHash=data["configHash"],
        )


@dataclass(frozen=True)
class RepositoryRecordCapabilityCeiling:
    """
    Generated from schemas/registry/repository-record.schema.json.

    Source: schemas/registry/repository-record.schema.json
    """

    maxSideEffect: SideEffectClass
    allowedTools: list[str]
    networkAllowlist: list[str]
    pathScope: dict[str, PathScopeEntry]
    approvalOverrides: list[SideEffectClass] | None = None
    budgetCeiling: RepositoryRecordCapabilityCeilingBudgetCeiling | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["maxSideEffect"] = self.maxSideEffect.value
        data["allowedTools"] = list(self.allowedTools)
        data["networkAllowlist"] = list(self.networkAllowlist)
        data["pathScope"] = {_k: _v.to_dict() for _k, _v in self.pathScope.items()}
        if self.approvalOverrides is not None:
            data["approvalOverrides"] = [_i.value for _i in self.approvalOverrides]
        if self.budgetCeiling is not None:
            data["budgetCeiling"] = self.budgetCeiling.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> RepositoryRecordCapabilityCeiling:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxSideEffect=SideEffectClass(data["maxSideEffect"]),
            allowedTools=list(data["allowedTools"]),
            networkAllowlist=list(data["networkAllowlist"]),
            pathScope={_k: PathScopeEntry.from_dict(_v) for _k, _v in data["pathScope"].items()},
            approvalOverrides=None if data.get("approvalOverrides") is None else [SideEffectClass(_i) for _i in data["approvalOverrides"]],
            budgetCeiling=None if data.get("budgetCeiling") is None else RepositoryRecordCapabilityCeilingBudgetCeiling.from_dict(data["budgetCeiling"]),
        )


@dataclass(frozen=True)
class RepositoryRecordCapabilityCeilingBudgetCeiling:
    """
    Generated from schemas/registry/repository-record.schema.json.

    Source: schemas/registry/repository-record.schema.json
    """

    maxCostUnitsPerRun: int | None = None
    maxInvocationsPerAttempt: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.maxCostUnitsPerRun is not None:
            data["maxCostUnitsPerRun"] = self.maxCostUnitsPerRun
        if self.maxInvocationsPerAttempt is not None:
            data["maxInvocationsPerAttempt"] = self.maxInvocationsPerAttempt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> RepositoryRecordCapabilityCeilingBudgetCeiling:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            maxCostUnitsPerRun=None if data.get("maxCostUnitsPerRun") is None else data["maxCostUnitsPerRun"],
            maxInvocationsPerAttempt=None if data.get("maxInvocationsPerAttempt") is None else data["maxInvocationsPerAttempt"],
        )


@dataclass(frozen=True)
class RepositoryRecordContextPolicy:
    """
    Generated from schemas/registry/repository-record.schema.json.

    Source: schemas/registry/repository-record.schema.json
    """

    includePaths: list[str]
    excludePaths: list[str]
    maxContextBudget: int
    instructionFiles: list[str] | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["includePaths"] = list(self.includePaths)
        data["excludePaths"] = list(self.excludePaths)
        data["maxContextBudget"] = self.maxContextBudget
        if self.instructionFiles is not None:
            data["instructionFiles"] = list(self.instructionFiles)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> RepositoryRecordContextPolicy:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            includePaths=list(data["includePaths"]),
            excludePaths=list(data["excludePaths"]),
            maxContextBudget=data["maxContextBudget"],
            instructionFiles=None if data.get("instructionFiles") is None else list(data["instructionFiles"]),
        )


@dataclass(frozen=True)
class RepositoryRecordVerification:
    """
    Generated from schemas/registry/repository-record.schema.json.

    Source: schemas/registry/repository-record.schema.json
    """

    requiredChecks: list[str]

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["requiredChecks"] = list(self.requiredChecks)
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> RepositoryRecordVerification:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            requiredChecks=list(data["requiredChecks"]),
        )


@dataclass(frozen=True)
class ResourceLimits:
    """
    Generated from schemas/common/common.schema.json.

    Source: schemas/common/common.schema.json
    """

    cpuMillis: int
    memoryBytes: int
    pids: int
    diskBytes: int

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["cpuMillis"] = self.cpuMillis
        data["memoryBytes"] = self.memoryBytes
        data["pids"] = self.pids
        data["diskBytes"] = self.diskBytes
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ResourceLimits:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            cpuMillis=data["cpuMillis"],
            memoryBytes=data["memoryBytes"],
            pids=data["pids"],
            diskBytes=data["diskBytes"],
        )


@dataclass(frozen=True)
class ResourceUsage:
    """
    Generated from schemas/common/common.schema.json.

    Source: schemas/common/common.schema.json
    """

    cpuMillis: int | None = None
    maxMemoryBytes: int | None = None
    diskBytesWritten: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.cpuMillis is not None:
            data["cpuMillis"] = self.cpuMillis
        if self.maxMemoryBytes is not None:
            data["maxMemoryBytes"] = self.maxMemoryBytes
        if self.diskBytesWritten is not None:
            data["diskBytesWritten"] = self.diskBytesWritten
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ResourceUsage:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            cpuMillis=None if data.get("cpuMillis") is None else data["cpuMillis"],
            maxMemoryBytes=None if data.get("maxMemoryBytes") is None else data["maxMemoryBytes"],
            diskBytesWritten=None if data.get("diskBytesWritten") is None else data["diskBytesWritten"],
        )


@dataclass(frozen=True)
class Tool:
    """
    The declared contract of a versioned tool (ADR-0005 §2). Tools are data and are immutable
    per version. The agent runtime holds tool definitions only, never implementations, and
    cannot invoke a tool by any path other than the control-plane choke point.

    Source: schemas/tools/tool.schema.json
    """

    schemaVersion: str
    id_: str
    version: str
    title: str
    description: str
    inputSchema: dict[str, Any]
    outputSchema: dict[str, Any]
    sideEffectClass: SideEffectClass
    idempotency: ToolIdempotency
    approvalPolicy: ToolApprovalPolicy
    timeoutMs: int
    scopeRequirements: ToolScopeRequirements | None = None
    costHint: ToolCostHint | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["id"] = self.id_
        data["version"] = self.version
        data["title"] = self.title
        data["description"] = self.description
        data["inputSchema"] = self.inputSchema
        data["outputSchema"] = self.outputSchema
        data["sideEffectClass"] = self.sideEffectClass.value
        data["idempotency"] = self.idempotency.value
        data["approvalPolicy"] = self.approvalPolicy.to_dict()
        data["timeoutMs"] = self.timeoutMs
        if self.scopeRequirements is not None:
            data["scopeRequirements"] = self.scopeRequirements.to_dict()
        if self.costHint is not None:
            data["costHint"] = self.costHint.value
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> Tool:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            id_=data["id"],
            version=data["version"],
            title=data["title"],
            description=data["description"],
            inputSchema=data["inputSchema"],
            outputSchema=data["outputSchema"],
            sideEffectClass=SideEffectClass(data["sideEffectClass"]),
            idempotency=ToolIdempotency(data["idempotency"]),
            approvalPolicy=ToolApprovalPolicy.from_dict(data["approvalPolicy"]),
            timeoutMs=data["timeoutMs"],
            scopeRequirements=None if data.get("scopeRequirements") is None else ToolScopeRequirements.from_dict(data["scopeRequirements"]),
            costHint=None if data.get("costHint") is None else ToolCostHint(data["costHint"]),
        )


@dataclass(frozen=True)
class ToolApprovalPolicy:
    """
    ADR-0005 §2. IRREVERSIBLE tools always require approval regardless of this field; policy may
    strengthen the requirement but never weaken it. Union of mutually exclusive branches;
    exactly one branch's required fields are present in a valid document, which the JSON Schema
    enforces and this type does not.

    Source: schemas/tools/tool.schema.json
    """

    mode: ToolApprovalPolicyMode | None = None
    predicate: ConditionExpression | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.mode is not None:
            data["mode"] = self.mode.value
        if self.predicate is not None:
            data["predicate"] = self.predicate.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ToolApprovalPolicy:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            mode=None if data.get("mode") is None else ToolApprovalPolicyMode(data["mode"]),
            predicate=None if data.get("predicate") is None else ConditionExpression.from_dict(data["predicate"]),
        )


@dataclass(frozen=True)
class ToolRequest:
    """
    A request to invoke a Tool, submitted by the agent runtime to the control-plane tool layer
    (ADR-0005 §4). It is a request, not an invocation: nothing has been authorized when this
    message is constructed, and the model's proposal and the platform's decision are
    deliberately separate documents. Authorization, approval and execution all happen on the
    control-plane side of this boundary, so the runtime cannot widen its own authority by asking
    differently.

    Source: schemas/tools/tool-request.schema.json
    """

    protocolVersion: str
    toolCallId: str
    toolId: str
    toolVersion: str
    callbackToken: str
    input_: dict[str, Any]
    requestedAt: str
    idempotencyKey: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["toolCallId"] = self.toolCallId
        data["toolId"] = self.toolId
        data["toolVersion"] = self.toolVersion
        data["callbackToken"] = self.callbackToken
        data["input"] = self.input_
        data["requestedAt"] = self.requestedAt
        if self.idempotencyKey is not None:
            data["idempotencyKey"] = self.idempotencyKey
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ToolRequest:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            toolCallId=data["toolCallId"],
            toolId=data["toolId"],
            toolVersion=data["toolVersion"],
            callbackToken=data["callbackToken"],
            input_=data["input"],
            requestedAt=data["requestedAt"],
            idempotencyKey=None if data.get("idempotencyKey") is None else data["idempotencyKey"],
        )


@dataclass(frozen=True)
class ToolResult:
    """
    The outcome of a ToolRequest, returned by the control-plane tool layer (ADR-0005 §4). A
    denial, a pending approval and a failure are all results — the runtime is told what happened
    rather than left to infer it from an error. Output is returned as data and is treated as
    UNTRUSTED by the agent runtime: it is rendered where the model protocol expects tool output,
    never where it expects platform instructions.

    Source: schemas/tools/tool-result.schema.json
    """

    protocolVersion: str
    toolCallId: str
    status: ToolResultStatus
    sideEffectClass: SideEffectClass
    completedAt: str
    output: dict[str, Any] | None = None
    outputRef: ArtifactRef | None = None
    denial: ToolResultDenial | None = None
    approvalId: str | None = None
    failure: ToolResultFailure | None = None
    durationMs: int | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["toolCallId"] = self.toolCallId
        data["status"] = self.status.value
        data["sideEffectClass"] = self.sideEffectClass.value
        data["completedAt"] = self.completedAt
        if self.output is not None:
            data["output"] = self.output
        if self.outputRef is not None:
            data["outputRef"] = self.outputRef.to_dict()
        if self.denial is not None:
            data["denial"] = self.denial.to_dict()
        if self.approvalId is not None:
            data["approvalId"] = self.approvalId
        if self.failure is not None:
            data["failure"] = self.failure.to_dict()
        if self.durationMs is not None:
            data["durationMs"] = self.durationMs
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ToolResult:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            toolCallId=data["toolCallId"],
            status=ToolResultStatus(data["status"]),
            sideEffectClass=SideEffectClass(data["sideEffectClass"]),
            completedAt=data["completedAt"],
            output=None if data.get("output") is None else data["output"],
            outputRef=None if data.get("outputRef") is None else ArtifactRef.from_dict(data["outputRef"]),
            denial=None if data.get("denial") is None else ToolResultDenial.from_dict(data["denial"]),
            approvalId=None if data.get("approvalId") is None else data["approvalId"],
            failure=None if data.get("failure") is None else ToolResultFailure.from_dict(data["failure"]),
            durationMs=None if data.get("durationMs") is None else data["durationMs"],
        )


@dataclass(frozen=True)
class ToolResultDenial:
    """
    Present when status is DENIED. The reason is deliberately model-facing and specific enough
    to be actionable, but never discloses what the grant would have permitted — that would turn
    a denial into an enumeration oracle for the agent's own boundary.

    Source: schemas/tools/tool-result.schema.json
    """

    reason: ToolResultDenialReason
    message: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["reason"] = self.reason.value
        data["message"] = self.message
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ToolResultDenial:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            reason=ToolResultDenialReason(data["reason"]),
            message=data["message"],
        )


@dataclass(frozen=True)
class ToolResultFailure:
    """
    Generated from schemas/tools/tool-result.schema.json.

    Source: schemas/tools/tool-result.schema.json
    """

    category: FailureCategory
    message: str
    detailsRef: ArtifactRef | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["category"] = self.category.value
        data["message"] = self.message
        if self.detailsRef is not None:
            data["detailsRef"] = self.detailsRef.to_dict()
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ToolResultFailure:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            category=FailureCategory(data["category"]),
            message=data["message"],
            detailsRef=None if data.get("detailsRef") is None else ArtifactRef.from_dict(data["detailsRef"]),
        )


@dataclass(frozen=True)
class ToolScopeRequirements:
    """
    Which scopes a grant must contain for this tool to be invocable.

    Source: schemas/tools/tool.schema.json
    """

    repository: bool | None = None
    path: bool | None = None
    externalProject: bool | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        if self.repository is not None:
            data["repository"] = self.repository
        if self.path is not None:
            data["path"] = self.path
        if self.externalProject is not None:
            data["externalProject"] = self.externalProject
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> ToolScopeRequirements:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            repository=None if data.get("repository") is None else data["repository"],
            path=None if data.get("path") is None else data["path"],
            externalProject=None if data.get("externalProject") is None else data["externalProject"],
        )


@dataclass(frozen=True)
class Workspace:
    """
    The durable record of an isolated, reconstructible, credential-free materialisation of one
    or more repositories for exactly one GraphRun (ADR-0003 §7, ADR-0006). WorkspaceSpec is the
    creation input; this is the record. The record exists because a workspace outlives the
    process that created it: after a crash the platform must be able to tell whether a workspace
    is still leased, reclaimable or quarantined, and losing that knowledge means either leaking
    disk or destroying a workspace another process is using. Layout is fixed by ADR-0023 §1: one
    directory per repositoryId directly beneath the root.

    Source: schemas/execution/workspace.schema.json
    """

    schemaVersion: str
    workspaceId: str
    runId: str
    status: WorkspaceStatus
    toolImageDigest: str
    repositories: list[RepoRef]
    executionMode: WorkspaceExecutionMode
    quotaBytes: int
    createdAt: str
    lease: WorkspaceLease | None = None
    lastUsedAt: str | None = None
    destroyedAt: str | None = None

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["schemaVersion"] = self.schemaVersion
        data["workspaceId"] = self.workspaceId
        data["runId"] = self.runId
        data["status"] = self.status.value
        data["toolImageDigest"] = self.toolImageDigest
        data["repositories"] = [_i.to_dict() for _i in self.repositories]
        data["executionMode"] = self.executionMode.value
        data["quotaBytes"] = self.quotaBytes
        data["createdAt"] = self.createdAt
        if self.lease is not None:
            data["lease"] = self.lease.to_dict()
        if self.lastUsedAt is not None:
            data["lastUsedAt"] = self.lastUsedAt
        if self.destroyedAt is not None:
            data["destroyedAt"] = self.destroyedAt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> Workspace:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            schemaVersion=data["schemaVersion"],
            workspaceId=data["workspaceId"],
            runId=data["runId"],
            status=WorkspaceStatus(data["status"]),
            toolImageDigest=data["toolImageDigest"],
            repositories=[RepoRef.from_dict(_i) for _i in data["repositories"]],
            executionMode=WorkspaceExecutionMode(data["executionMode"]),
            quotaBytes=data["quotaBytes"],
            createdAt=data["createdAt"],
            lease=None if data.get("lease") is None else WorkspaceLease.from_dict(data["lease"]),
            lastUsedAt=None if data.get("lastUsedAt") is None else data["lastUsedAt"],
            destroyedAt=None if data.get("destroyedAt") is None else data["destroyedAt"],
        )


@dataclass(frozen=True)
class WorkspaceLease:
    """
    Exclusive. Two runs never share a checkout, and no run can reach another run's workspace or
    the developer's own repositories. Expiry makes the workspace collectable rather than
    silently shared.

    Source: schemas/execution/workspace.schema.json
    """

    holderId: str
    expiresAt: str

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["holderId"] = self.holderId
        data["expiresAt"] = self.expiresAt
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> WorkspaceLease:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            holderId=data["holderId"],
            expiresAt=data["expiresAt"],
        )


@dataclass(frozen=True)
class WorkspaceSpec:
    """
    Passed to createWorkspace() (ADR-0003 §1). One workspace per run, not per node (ADR-0003
    §7), held under an exclusive lease so two runs never share a checkout.

    Source: schemas/execution/workspace-spec.schema.json
    """

    protocolVersion: str
    runId: str
    repositories: list[RepoRef]
    toolImage: str
    resourceLimits: ResourceLimits
    leaseTtlMs: int

    def to_dict(self) -> dict[str, Any]:
        """Absent optional fields are omitted, never emitted as null (ADR-0020 §3)."""
        data: dict[str, Any] = {}
        data["protocolVersion"] = self.protocolVersion
        data["runId"] = self.runId
        data["repositories"] = [_i.to_dict() for _i in self.repositories]
        data["toolImage"] = self.toolImage
        data["resourceLimits"] = self.resourceLimits.to_dict()
        data["leaseTtlMs"] = self.leaseTtlMs
        return data

    @classmethod
    def from_dict(cls, data: dict[str, Any]) -> WorkspaceSpec:
        """Performs no validation (ADR-0024 §4)."""
        return cls(
            protocolVersion=data["protocolVersion"],
            runId=data["runId"],
            repositories=[RepoRef.from_dict(_i) for _i in data["repositories"]],
            toolImage=data["toolImage"],
            resourceLimits=ResourceLimits.from_dict(data["resourceLimits"]),
            leaseTtlMs=data["leaseTtlMs"],
        )

