{
  "$schema": "../../tools/tool.schema.json",
  "$expect": "ADR-0005 §3 and §8: EXTERNAL_WRITE requires an idempotency key, so that running the same node twice leaves one branch, one pull request and one comment.",
  "$expectKeyword": "enum",
  "id": "jira.issue.comment",
  "version": "1.0.0",
  "title": "Comment on an issue",
  "description": "Adds a comment to a Jira issue.",
  "inputSchema": {
    "type": "object",
    "properties": { "body": { "type": "string" } },
    "required": ["body"],
    "additionalProperties": false
  },
  "outputSchema": { "type": "object" },
  "sideEffectClass": "EXTERNAL_WRITE",
  "idempotency": "NONE",
  "approvalPolicy": { "mode": "NEVER" },
  "timeoutMs": 30000
}
